//! Generated by fltk gen-rust-unparser from `fltk/fegen/fegen.fltkg`. Do not edit.
#![allow(non_snake_case)]

use fltk_unparser_core::{DocAccumulator, UnparseResult};
use fltk_unparser_core::Doc;
#[cfg(feature = "python")]
use fltk_unparser_core::{Renderer, RendererConfig, resolve_spacing_specs};

use super::cst;

/// Generated unparser.
///
/// The `FormatterConfig` is baked into the per-rule methods at generation time;
/// render width/indent is supplied at render time, not construction.
#[derive(Default)]
pub struct Unparser;

impl Unparser {
    pub fn new() -> Self {
        Unparser
    }
}
impl Unparser {
    #[allow(dead_code)]
    fn _has_preservable_trivia(&self, trivia_node: &cst::Trivia) -> bool {
        for child in trivia_node.children() {
            if let cst::TriviaChild::BlockComment(_) | cst::TriviaChild::LineComment(_) = &child.1 {
                return true;
            }
        }
        false
    }
    #[allow(dead_code)]
    fn _count_newlines_in_trivia(&self, trivia: &cst::Trivia) -> usize {
        let mut count = 0usize;
        for child in trivia.children() {
            if let cst::TriviaChild::Span(span) = &child.1 {
                count += span.text_str().map(|t| t.matches('\n').count()).unwrap_or(0);
            }
        }
        count
    }
    pub fn unparse_grammar(&self, node: &cst::Grammar) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse_grammar__alt0(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_grammar__alt0(&self, node: &cst::Grammar, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::GrammarChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        let r = self.unparse_grammar__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_grammar__alt0__item0(&self, node: &cst::Grammar, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut current_pos = pos;
        let mut acc = acc;
        let mut match_count = 0usize;
        while current_pos < node.children().len() {
            let Some(r) = self.unparse_grammar__alt0__item0__inner(node, current_pos, acc.clone()) else {
                break;
            };
            acc = r.accumulator;
            current_pos = r.new_pos;
            match_count += 1;
        }
        if match_count == 0 {
            return None;
        }
        Some(UnparseResult::new(acc, current_pos))
    }

    fn unparse_grammar__alt0__item0__inner(&self, node: &cst::Grammar, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::GrammarLabel::Rule) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::GrammarChild::Rule(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_rule(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse_rule(&self, node: &cst::Rule) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        let acc = acc.push_group();
        if let Some(r) = self.unparse_rule__alt0(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_rule__alt0(&self, node: &cst::Rule, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.push_group();
        let r = self.unparse_rule__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Nbsp));
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::RuleChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        let r = self.unparse_rule__alt0__item1(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        acc = acc.pop_group();
        acc = acc.push_nest(1);
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::RuleChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        let r = self.unparse_rule__alt0__item2(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::RuleChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_rule__alt0__item3(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::HardLine { blank_lines: 0 }));
        acc = acc.pop_nest();
        acc = acc.pop_group();
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::RuleChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_rule__alt0__item0(&self, node: &cst::Rule, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::RuleLabel::Name) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::RuleChild::Identifier(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_identifier(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_rule__alt0__item1(&self, _node: &cst::Rule, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let acc = acc.add_non_trivia(fltk_unparser_core::text(":="));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_rule__alt0__item2(&self, node: &cst::Rule, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::RuleLabel::Alternatives) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::RuleChild::Alternatives(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_alternatives(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_rule__alt0__item3(&self, _node: &cst::Rule, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let acc = acc.add_non_trivia(fltk_unparser_core::text(";"));
        Some(UnparseResult::new(acc, pos))
    }
    pub fn unparse_alternatives(&self, node: &cst::Alternatives) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        let acc = acc.push_group();
        if let Some(r) = self.unparse_alternatives__alt0(node, 0, acc) {
            let acc = r.accumulator.pop_group();
            return Some(UnparseResult::new(acc, r.new_pos));
        }
        None
    }

    fn unparse_alternatives__alt0(&self, node: &cst::Alternatives, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_alternatives__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::AlternativesChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        if let Some(r) = self.unparse_alternatives__alt0__item1(node, pos, acc.clone()) {
            pos = r.new_pos;
            acc = r.accumulator;
        }
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_alternatives__alt0__item0(&self, node: &cst::Alternatives, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::AlternativesLabel::Items) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::AlternativesChild::Items(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_items(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_alternatives__alt0__item1(&self, node: &cst::Alternatives, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut current_pos = pos;
        let mut acc = acc;
        while current_pos < node.children().len() {
            let Some(r) = self.unparse_alternatives__alt0__item1__inner(node, current_pos, acc.clone()) else {
                break;
            };
            acc = r.accumulator;
            current_pos = r.new_pos;
        }
        Some(UnparseResult::new(acc, current_pos))
    }

    fn unparse_alternatives__alt0__item1__inner(&self, node: &cst::Alternatives, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        self.unparse_alternatives__alt0__item1__inner__alts(node, pos, acc)
    }

    fn unparse_alternatives__alt0__item1__inner__alts(&self, node: &cst::Alternatives, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        if let Some(r) = self.unparse_alternatives__alt0__item1__inner__alts__alt0(node, pos, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_alternatives__alt0__item1__inner__alts__alt0(&self, node: &cst::Alternatives, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Line));
        let r = self.unparse_alternatives__alt0__item1__inner__alts__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Nbsp));
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::AlternativesChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        let r = self.unparse_alternatives__alt0__item1__inner__alts__alt0__item1(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::AlternativesChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_alternatives__alt0__item1__inner__alts__alt0__item0(&self, _node: &cst::Alternatives, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let acc = acc.add_non_trivia(fltk_unparser_core::text("|"));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_alternatives__alt0__item1__inner__alts__alt0__item1(&self, node: &cst::Alternatives, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::AlternativesLabel::Items) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::AlternativesChild::Items(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_items(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse_items(&self, node: &cst::Items) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        let acc = acc.push_group();
        if let Some(r) = self.unparse_items__alt0(node, 0, acc) {
            let acc = r.accumulator.pop_group();
            return Some(UnparseResult::new(acc, r.new_pos));
        }
        None
    }

    fn unparse_items__alt0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        if let Some(r) = self.unparse_items__alt0__item0(node, pos, acc.clone()) {
            pos = r.new_pos;
            acc = r.accumulator;
        }
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::ItemsChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        let r = self.unparse_items__alt0__item1(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Nbsp));
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::ItemsChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        if let Some(r) = self.unparse_items__alt0__item2(node, pos, acc.clone()) {
            pos = r.new_pos;
            acc = r.accumulator;
        }
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::ItemsChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        if let Some(r) = self.unparse_items__alt0__item3(node, pos, acc.clone()) {
            pos = r.new_pos;
            acc = r.accumulator;
        }
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::ItemsChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        self.unparse_items__alt0__item0__alts(node, pos, acc)
    }

    fn unparse_items__alt0__item0__alts(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        if let Some(r) = self.unparse_items__alt0__item0__alts__alt0(node, pos, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_items__alt0__item0__alts__alt1(node, pos, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_items__alt0__item0__alts__alt2(node, pos, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_items__alt0__item0__alts__alt0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_items__alt0__item0__alts__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item0__alts__alt0__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::NoWs) {
            return None;
        }
        match &child_tuple.1 {
            cst::ItemsChild::Span(_) => {}
            _ => return None,
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("."));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item0__alts__alt1(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_items__alt0__item0__alts__alt1__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item0__alts__alt1__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::WsAllowed) {
            return None;
        }
        match &child_tuple.1 {
            cst::ItemsChild::Span(_) => {}
            _ => return None,
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text(","));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item0__alts__alt2(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_items__alt0__item0__alts__alt2__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item0__alts__alt2__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::WsRequired) {
            return None;
        }
        match &child_tuple.1 {
            cst::ItemsChild::Span(_) => {}
            _ => return None,
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text(":"));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item1(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::Item) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::ItemsChild::Item(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_item(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item2(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut current_pos = pos;
        let mut acc = acc;
        while current_pos < node.children().len() {
            let Some(r) = self.unparse_items__alt0__item2__inner(node, current_pos, acc.clone()) else {
                break;
            };
            acc = r.accumulator;
            current_pos = r.new_pos;
        }
        Some(UnparseResult::new(acc, current_pos))
    }

    fn unparse_items__alt0__item2__inner(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        self.unparse_items__alt0__item2__inner__alts(node, pos, acc)
    }

    fn unparse_items__alt0__item2__inner__alts(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        if let Some(r) = self.unparse_items__alt0__item2__inner__alts__alt0(node, pos, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_items__alt0__item2__inner__alts__alt0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_items__alt0__item2__inner__alts__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::ItemsChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        let r = self.unparse_items__alt0__item2__inner__alts__alt0__item1(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Nbsp));
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::ItemsChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item2__inner__alts__alt0__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        self.unparse_items__alt0__item2__inner__alts__alt0__item0__alts(node, pos, acc)
    }

    fn unparse_items__alt0__item2__inner__alts__alt0__item0__alts(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        if let Some(r) = self.unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt0(node, pos, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt1(node, pos, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt2(node, pos, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt0__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::NoWs) {
            return None;
        }
        match &child_tuple.1 {
            cst::ItemsChild::Span(_) => {}
            _ => return None,
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("."));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt1(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt1__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt1__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::WsAllowed) {
            return None;
        }
        match &child_tuple.1 {
            cst::ItemsChild::Span(_) => {}
            _ => return None,
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text(","));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt2(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt2__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item2__inner__alts__alt0__item0__alts__alt2__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::WsRequired) {
            return None;
        }
        match &child_tuple.1 {
            cst::ItemsChild::Span(_) => {}
            _ => return None,
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text(":"));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item2__inner__alts__alt0__item1(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::Item) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::ItemsChild::Item(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_item(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item3(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        self.unparse_items__alt0__item3__alts(node, pos, acc)
    }

    fn unparse_items__alt0__item3__alts(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        if let Some(r) = self.unparse_items__alt0__item3__alts__alt0(node, pos, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_items__alt0__item3__alts__alt1(node, pos, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_items__alt0__item3__alts__alt2(node, pos, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_items__alt0__item3__alts__alt0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_items__alt0__item3__alts__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item3__alts__alt0__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::NoWs) {
            return None;
        }
        match &child_tuple.1 {
            cst::ItemsChild::Span(_) => {}
            _ => return None,
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("."));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item3__alts__alt1(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_items__alt0__item3__alts__alt1__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item3__alts__alt1__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::WsAllowed) {
            return None;
        }
        match &child_tuple.1 {
            cst::ItemsChild::Span(_) => {}
            _ => return None,
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text(","));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_items__alt0__item3__alts__alt2(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_items__alt0__item3__alts__alt2__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_items__alt0__item3__alts__alt2__item0(&self, node: &cst::Items, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemsLabel::WsRequired) {
            return None;
        }
        match &child_tuple.1 {
            cst::ItemsChild::Span(_) => {}
            _ => return None,
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text(":"));
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse_item(&self, node: &cst::Item) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse_item__alt0(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_item__alt0(&self, node: &cst::Item, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        if let Some(r) = self.unparse_item__alt0__item0(node, pos, acc.clone()) {
            pos = r.new_pos;
            acc = r.accumulator;
        }
        if let Some(r) = self.unparse_item__alt0__item1(node, pos, acc.clone()) {
            pos = r.new_pos;
            acc = r.accumulator;
        }
        let r = self.unparse_item__alt0__item2(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        if let Some(r) = self.unparse_item__alt0__item3(node, pos, acc.clone()) {
            pos = r.new_pos;
            acc = r.accumulator;
        }
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::ItemChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_item__alt0__item0(&self, node: &cst::Item, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        self.unparse_item__alt0__item0__alts(node, pos, acc)
    }

    fn unparse_item__alt0__item0__alts(&self, node: &cst::Item, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        if let Some(r) = self.unparse_item__alt0__item0__alts__alt0(node, pos, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_item__alt0__item0__alts__alt0(&self, node: &cst::Item, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_item__alt0__item0__alts__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nbsp));
        let r = self.unparse_item__alt0__item0__alts__alt0__item1(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Nil));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_item__alt0__item0__alts__alt0__item0(&self, node: &cst::Item, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemLabel::Label) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::ItemChild::Identifier(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_identifier(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_item__alt0__item0__alts__alt0__item1(&self, _node: &cst::Item, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let acc = acc.add_non_trivia(fltk_unparser_core::text(":"));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_item__alt0__item1(&self, node: &cst::Item, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemLabel::Disposition) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::ItemChild::Disposition(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_disposition(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_item__alt0__item2(&self, node: &cst::Item, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemLabel::Term) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::ItemChild::Term(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_term(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_item__alt0__item3(&self, node: &cst::Item, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::ItemLabel::Quantifier) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::ItemChild::Quantifier(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_quantifier(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse_term(&self, node: &cst::Term) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        let acc = acc.push_group();
        if let Some(r) = self.unparse_term__alt0(node, 0, acc.clone()) {
            let acc = r.accumulator.pop_group();
            return Some(UnparseResult::new(acc, r.new_pos));
        }
        if let Some(r) = self.unparse_term__alt1(node, 0, acc.clone()) {
            let acc = r.accumulator.pop_group();
            return Some(UnparseResult::new(acc, r.new_pos));
        }
        if let Some(r) = self.unparse_term__alt2(node, 0, acc.clone()) {
            let acc = r.accumulator.pop_group();
            return Some(UnparseResult::new(acc, r.new_pos));
        }
        if let Some(r) = self.unparse_term__alt3(node, 0, acc) {
            let acc = r.accumulator.pop_group();
            return Some(UnparseResult::new(acc, r.new_pos));
        }
        None
    }

    fn unparse_term__alt0(&self, node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_term__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_term__alt0__item0(&self, node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::TermLabel::Identifier) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::TermChild::Identifier(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_identifier(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_term__alt1(&self, node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_term__alt1__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_term__alt1__item0(&self, node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::TermLabel::Literal) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::TermChild::Literal(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_literal(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_term__alt2(&self, node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nil));
        let r = self.unparse_term__alt2__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Nil));
        let r = self.unparse_term__alt2__item1(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Nil));
        let r = self.unparse_term__alt2__item2(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Nil));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_term__alt2__item0(&self, _node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let acc = acc.add_non_trivia(fltk_unparser_core::text("/"));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_term__alt2__item1(&self, node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::TermLabel::Regex) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::TermChild::RawString(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_raw_string(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_term__alt2__item2(&self, _node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let acc = acc.add_non_trivia(fltk_unparser_core::text("/"));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_term__alt3(&self, node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_term__alt3__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        acc = acc.add_non_trivia(fltk_unparser_core::after_spec(Doc::Line));
        acc = acc.push_nest(1);
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::TermChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        let r = self.unparse_term__alt3__item1(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        if !acc.last_was_trivia() {
            if pos < node.children().len() {
                match &node.children()[pos].1 {
                    cst::TermChild::Trivia(trivia_shared) => {
                        let trivia_node = trivia_shared.read();
                        if self._has_preservable_trivia(&trivia_node) {
                            if let Some(trivia_result) = self.unparse__trivia(&trivia_node) {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(None, Some(trivia_result.accumulator.doc()), false));
                            }
                        } else {
                            let newline_count = self._count_newlines_in_trivia(&trivia_node);
                            if newline_count >= 2 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, false));
                            } else if newline_count >= 1 {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, false));
                            } else {
                                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                            }
                        }
                        pos += 1;
                    }
                    _ => {
                        acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
                    }
                }
            } else {
                acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nil), None, false));
            }
        }
        acc = acc.pop_nest();
        acc = acc.add_non_trivia(fltk_unparser_core::before_spec(Doc::Line));
        let r = self.unparse_term__alt3__item2(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_term__alt3__item0(&self, _node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let acc = acc.add_non_trivia(fltk_unparser_core::text("("));
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_term__alt3__item1(&self, node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::TermLabel::Alternatives) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::TermChild::Alternatives(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_alternatives(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_term__alt3__item2(&self, _node: &cst::Term, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let acc = acc.add_non_trivia(fltk_unparser_core::text(")"));
        Some(UnparseResult::new(acc, pos))
    }
    pub fn unparse_disposition(&self, node: &cst::Disposition) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse_disposition__alt0(node, 0, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_disposition__alt1(node, 0, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_disposition__alt2(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_disposition__alt0(&self, node: &cst::Disposition, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_disposition__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_disposition__alt0__item0(&self, node: &cst::Disposition, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::DispositionLabel::Suppress) {
            return None;
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("%"));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_disposition__alt1(&self, node: &cst::Disposition, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_disposition__alt1__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_disposition__alt1__item0(&self, node: &cst::Disposition, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::DispositionLabel::Include) {
            return None;
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("$"));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_disposition__alt2(&self, node: &cst::Disposition, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_disposition__alt2__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_disposition__alt2__item0(&self, node: &cst::Disposition, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::DispositionLabel::Inline) {
            return None;
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("!"));
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse_quantifier(&self, node: &cst::Quantifier) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse_quantifier__alt0(node, 0, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_quantifier__alt1(node, 0, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse_quantifier__alt2(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_quantifier__alt0(&self, node: &cst::Quantifier, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_quantifier__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_quantifier__alt0__item0(&self, node: &cst::Quantifier, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::QuantifierLabel::Optional) {
            return None;
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("?"));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_quantifier__alt1(&self, node: &cst::Quantifier, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_quantifier__alt1__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_quantifier__alt1__item0(&self, node: &cst::Quantifier, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::QuantifierLabel::OneOrMore) {
            return None;
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("+"));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_quantifier__alt2(&self, node: &cst::Quantifier, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_quantifier__alt2__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_quantifier__alt2__item0(&self, node: &cst::Quantifier, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::QuantifierLabel::ZeroOrMore) {
            return None;
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("*"));
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse_identifier(&self, node: &cst::Identifier) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse_identifier__alt0(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_identifier__alt0(&self, node: &cst::Identifier, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_identifier__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_identifier__alt0__item0(&self, node: &cst::Identifier, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::IdentifierLabel::Name) {
            return None;
        }
        let cst::IdentifierChild::Span(span) = &child_tuple.1;
        let text = span.text()?;
        let acc = acc.add_non_trivia(fltk_unparser_core::text(text));
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse_raw_string(&self, node: &cst::RawString) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse_raw_string__alt0(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_raw_string__alt0(&self, node: &cst::RawString, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_raw_string__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_raw_string__alt0__item0(&self, node: &cst::RawString, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::RawStringLabel::Value) {
            return None;
        }
        let cst::RawStringChild::Span(span) = &child_tuple.1;
        let text = span.text()?;
        let acc = acc.add_non_trivia(fltk_unparser_core::text(text));
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse_literal(&self, node: &cst::Literal) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse_literal__alt0(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_literal__alt0(&self, node: &cst::Literal, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_literal__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_literal__alt0__item0(&self, node: &cst::Literal, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::LiteralLabel::Value) {
            return None;
        }
        let cst::LiteralChild::Span(span) = &child_tuple.1;
        let text = span.text()?;
        let acc = acc.add_non_trivia(fltk_unparser_core::text(text));
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse__trivia(&self, node: &cst::Trivia) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse__trivia__alt0(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse__trivia__alt0(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse__trivia__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse__trivia__alt0__item0(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut current_pos = pos;
        let mut acc = acc;
        let mut match_count = 0usize;
        while current_pos < node.children().len() {
            let Some(r) = self.unparse__trivia__alt0__item0__inner(node, current_pos, acc.clone()) else {
                break;
            };
            acc = r.accumulator;
            current_pos = r.new_pos;
            match_count += 1;
        }
        if match_count == 0 {
            return None;
        }
        Some(UnparseResult::new(acc, current_pos))
    }

    fn unparse__trivia__alt0__item0__inner(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        self.unparse__trivia__alt0__item0__inner__alts(node, pos, acc)
    }

    fn unparse__trivia__alt0__item0__inner__alts(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        if let Some(r) = self.unparse__trivia__alt0__item0__inner__alts__alt0(node, pos, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse__trivia__alt0__item0__inner__alts__alt1(node, pos, acc.clone()) {
            return Some(r);
        }
        if let Some(r) = self.unparse__trivia__alt0__item0__inner__alts__alt2(node, pos, acc) {
            return Some(r);
        }
        None
    }

    fn unparse__trivia__alt0__item0__inner__alts__alt0(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse__trivia__alt0__item0__inner__alts__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse__trivia__alt0__item0__inner__alts__alt0__item0(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::TriviaLabel::LineComment) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::TriviaChild::LineComment(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_line_comment(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse__trivia__alt0__item0__inner__alts__alt1(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        if let Some(r) = self.unparse__trivia__alt0__item0__inner__alts__alt1__item0(node, pos, acc.clone()) {
            pos = r.new_pos;
            acc = r.accumulator;
        }
        if pos < node.children().len() {
            if let (None, cst::TriviaChild::Span(span)) = &node.children()[pos] {
                pos += 1;
                let newline_count = span.text_str().map(|t| t.matches('\n').count()).unwrap_or(0);
                if newline_count >= 2 {
                    acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 1 }), None, true));
                } else if newline_count >= 1 {
                    acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::HardLine { blank_lines: 0 }), None, true));
                } else {
                    acc = acc.add_trivia(fltk_unparser_core::separator_spec(Some(Doc::Nbsp), None, true));
                }
            } else {
                return None;
            }
        } else {
            return None;
        }
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse__trivia__alt0__item0__inner__alts__alt1__item0(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::TriviaLabel::LineComment) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::TriviaChild::LineComment(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_line_comment(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse__trivia__alt0__item0__inner__alts__alt2(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse__trivia__alt0__item0__inner__alts__alt2__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse__trivia__alt0__item0__inner__alts__alt2__item0(&self, node: &cst::Trivia, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::TriviaLabel::BlockComment) {
            return None;
        }
        let shared = match &child_tuple.1 {
            cst::TriviaChild::BlockComment(shared) => shared,
            _ => return None,
        };
        let guard = shared.read();
        let child_result = self.unparse_block_comment(&guard)?;
        let acc = acc.add_accumulator(&child_result.accumulator);
        Some(UnparseResult::new(acc, pos + 1))
    }
    pub fn unparse_line_comment(&self, node: &cst::LineComment) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse_line_comment__alt0(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_line_comment__alt0(&self, node: &cst::LineComment, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_line_comment__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        let r = self.unparse_line_comment__alt0__item1(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        let r = self.unparse_line_comment__alt0__item2(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_line_comment__alt0__item0(&self, node: &cst::LineComment, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::LineCommentLabel::Prefix) {
            return None;
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("//"));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_line_comment__alt0__item1(&self, node: &cst::LineComment, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::LineCommentLabel::Content) {
            return None;
        }
        let cst::LineCommentChild::Span(span) = &child_tuple.1;
        let text = span.text()?;
        let acc = acc.add_non_trivia(fltk_unparser_core::text(text));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_line_comment__alt0__item2(&self, _node: &cst::LineComment, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let acc = acc.add_non_trivia(fltk_unparser_core::text("\u{0a}"));
        Some(UnparseResult::new(acc, pos))
    }
    pub fn unparse_block_comment(&self, node: &cst::BlockComment) -> Option<UnparseResult> {
        let acc = DocAccumulator::new();
        if let Some(r) = self.unparse_block_comment__alt0(node, 0, acc) {
            return Some(r);
        }
        None
    }

    fn unparse_block_comment__alt0(&self, node: &cst::BlockComment, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let mut pos = pos;
        let mut acc = acc;
        let r = self.unparse_block_comment__alt0__item0(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        let r = self.unparse_block_comment__alt0__item1(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        let r = self.unparse_block_comment__alt0__item2(node, pos, acc)?;
        pos = r.new_pos;
        acc = r.accumulator;
        Some(UnparseResult::new(acc, pos))
    }

    fn unparse_block_comment__alt0__item0(&self, node: &cst::BlockComment, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::BlockCommentLabel::Start) {
            return None;
        }
        let acc = acc.add_non_trivia(fltk_unparser_core::text("/*"));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_block_comment__alt0__item1(&self, node: &cst::BlockComment, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::BlockCommentLabel::Content) {
            return None;
        }
        let cst::BlockCommentChild::Span(span) = &child_tuple.1;
        let text = span.text()?;
        let acc = acc.add_non_trivia(fltk_unparser_core::text(text));
        Some(UnparseResult::new(acc, pos + 1))
    }

    fn unparse_block_comment__alt0__item2(&self, node: &cst::BlockComment, pos: usize, acc: DocAccumulator) -> Option<UnparseResult> {
        let children = node.children();
        if pos >= children.len() {
            return None;
        }
        let child_tuple = &children[pos];
        if child_tuple.0 != Some(cst::BlockCommentLabel::End) {
            return None;
        }
        let cst::BlockCommentChild::Span(span) = &child_tuple.1;
        let text = span.text()?;
        let acc = acc.add_non_trivia(fltk_unparser_core::text(text));
        Some(UnparseResult::new(acc, pos + 1))
    }
}
#[cfg(feature = "python")]
mod python_bindings {
    use pyo3::prelude::*;
    use super::cst;
    use super::Unparser;
    use super::{Renderer, RendererConfig, resolve_spacing_specs};

    /// Generated unparser, callable from Python.
    ///
    /// Each `unparse_{rule}` method runs the full pipeline (unparse -> resolve ->
    /// render) over a Rust-CST handle and returns the formatted string. It accepts
    /// only the Rust-backed `Py{ClassName}` handles, so a Python caller must pair
    /// this unparser with the Rust parser backend.
    #[pyclass(name = "Unparser")]
    pub struct PyUnparser {
        inner: Unparser,
    }

    /// A resolved formatting document, returned by the `unparse_{rule}_doc`
    /// methods so a caller can render it at multiple widths without re-walking the
    /// CST, or inspect it via `repr`.
    #[pyclass(name = "Doc", unsendable)]
    pub struct PyDoc {
        resolved: fltk_unparser_core::Doc,
    }

    #[pymethods]
    impl PyDoc {
        /// Render this resolved document to a string at the given width/indent.
        #[pyo3(signature = (max_width = 80, indent_width = 4))]
        fn render(&self, max_width: usize, indent_width: usize) -> String {
            let cfg = RendererConfig { indent_width, max_width };
            Renderer::new(cfg).render(&self.resolved)
        }

        fn __repr__(&self) -> String {
            format!("Doc({:?})", self.resolved)
        }
    }

    #[pymethods]
    impl PyUnparser {
        #[new]
        fn new() -> Self {
            PyUnparser {
                inner: Unparser::new(),
            }
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_grammar(&self, node: PyRef<'_, cst::PyGrammar>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_grammar(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_grammar_doc(&self, node: PyRef<'_, cst::PyGrammar>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_grammar(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_rule(&self, node: PyRef<'_, cst::PyRule>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_rule(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_rule_doc(&self, node: PyRef<'_, cst::PyRule>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_rule(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_alternatives(&self, node: PyRef<'_, cst::PyAlternatives>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_alternatives(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_alternatives_doc(&self, node: PyRef<'_, cst::PyAlternatives>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_alternatives(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_items(&self, node: PyRef<'_, cst::PyItems>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_items(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_items_doc(&self, node: PyRef<'_, cst::PyItems>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_items(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_item(&self, node: PyRef<'_, cst::PyItem>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_item(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_item_doc(&self, node: PyRef<'_, cst::PyItem>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_item(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_term(&self, node: PyRef<'_, cst::PyTerm>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_term(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_term_doc(&self, node: PyRef<'_, cst::PyTerm>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_term(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_disposition(&self, node: PyRef<'_, cst::PyDisposition>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_disposition(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_disposition_doc(&self, node: PyRef<'_, cst::PyDisposition>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_disposition(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_quantifier(&self, node: PyRef<'_, cst::PyQuantifier>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_quantifier(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_quantifier_doc(&self, node: PyRef<'_, cst::PyQuantifier>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_quantifier(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_identifier(&self, node: PyRef<'_, cst::PyIdentifier>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_identifier(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_identifier_doc(&self, node: PyRef<'_, cst::PyIdentifier>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_identifier(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_raw_string(&self, node: PyRef<'_, cst::PyRawString>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_raw_string(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_raw_string_doc(&self, node: PyRef<'_, cst::PyRawString>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_raw_string(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_literal(&self, node: PyRef<'_, cst::PyLiteral>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_literal(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_literal_doc(&self, node: PyRef<'_, cst::PyLiteral>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_literal(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse__trivia(&self, node: PyRef<'_, cst::PyTrivia>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse__trivia(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse__trivia_doc(&self, node: PyRef<'_, cst::PyTrivia>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse__trivia(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_line_comment(&self, node: PyRef<'_, cst::PyLineComment>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_line_comment(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_line_comment_doc(&self, node: PyRef<'_, cst::PyLineComment>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_line_comment(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }

        #[pyo3(signature = (node, max_width = 80, indent_width = 4))]
        fn unparse_block_comment(&self, node: PyRef<'_, cst::PyBlockComment>, max_width: usize, indent_width: usize) -> PyResult<Option<String>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_block_comment(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            let cfg = RendererConfig { indent_width, max_width };
            Ok(Some(Renderer::new(cfg).render(&resolved)))
        }

        fn unparse_block_comment_doc(&self, node: PyRef<'_, cst::PyBlockComment>) -> PyResult<Option<PyDoc>> {
            let guard = node.shared().read();
            let Some(r) = self.inner.unparse_block_comment(&guard) else {
                return Ok(None);
            };
            let resolved = resolve_spacing_specs(r.accumulator.doc());
            Ok(Some(PyDoc { resolved }))
        }
    }

    pub fn register_classes(module: &Bound<'_, pyo3::types::PyModule>) -> PyResult<()> {
        module.add_class::<PyUnparser>()?;
        module.add_class::<PyDoc>()?;
        Ok(())
    }
}
#[cfg(feature = "python")]
pub use python_bindings::register_classes;
