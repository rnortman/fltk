[pytest]
log_cli = True
#log_level = DEBUG
# tests/bazel_consumer imports Bazel-generated modules that only exist in the
# Bazel sandbox; exercised by `make bazel-consumer-check`, not pytest.
norecursedirs = bazel-* .claude bazel_consumer
