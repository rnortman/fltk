# Dispositions — design review round 2 (user notes)

Design: `design.md` (revised in place).
Notes: `notes-design-user-r2.md` (one finding: the answer to the design's open question).

user-r2-1 (merge the lanes onto one configuration — run `bazel test --config lint //...` and also `bazel build --config lint //...`; failure attribution is our job to surface, not a reason to keep the split):
- Disposition: Fixed
- Action: Open question 1 removed and replaced by a new "Part 4: one configuration for the whole root gate" section in `design.md` (Proposed approach). It specifies: `bazel-test`'s test invocation becomes `bazel test --config lint //...`; the guard cqueries in that recipe gain `--config lint` as well (a config-less cquery between lint-config builds would reintroduce the discard — fact-checked at the base commit: `bazel cquery --config lint "deps(//tests:rust_gate_lib)"` succeeds, since cquery inherits the `build:lint` block from `.bazelrc`, and the graph is pyo3-free); `bazel-lint` stays exactly `bazel build --config lint //...` as its own step, now a same-config cache-hit confirmation, per the user's "also run" instruction; the consumer lane is untouched (single-config workspace already, no alternation to remove). Failure attribution is addressed per the user's framing: Bazel names failing targets/actions, `--test_output=errors` (`.bazelrc:32`) prints failing test logs, and both check-common modes (Part 1) deliver that output — verbose streams it, quiet dumps the buffer on failure. Ripples applied: Files touched gains CLAUDE.md and the Makefile comment updates; Edge cases gains the cquery-under-lint-config and lint-failure-in-test-step entries; Test plan gains `test_root_gate_runs_one_configuration` and a manual-verification note (run the hot gate twice — the first run pays the one-time config-switch test re-run — and provoke a clippy failure to see attribution through `bazel-test`); expected hot-gate outcome updated to ~55-65s for Parts 3+4 together. Accepted consequences the design now states explicitly: dev-typed plain `bazel test //...` alternates config against the gate (the thrash moves to that boundary — the user's explicit trade), and the first post-landing gate run re-executes all tests once under the new configuration.
- Severity assessment: Directly removes the second measured cost (the analysis-cache discard worth roughly 20-40s per hot gate run across the test and lint steps); without it the gate keeps a permanent self-inflicted re-analysis tax.
