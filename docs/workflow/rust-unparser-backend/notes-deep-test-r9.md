test-1
File: fltk/fegen/test_genparser.py (no test) / fltk/fegen/genparser.py:529-535
What: The `except Exception` block guarding `stub_path.write_text(pyi_text)` (lines 529-535 of `genparser.py`) has no corresponding test. All five new CLI tests exercise success paths or the pre-write validation rejection (`--pyi-output` without `--protocol-module`). No test makes the `.pyi` destination unwritable after the `.rs` has been written.
Consequence: Three behaviors are unverified: (a) the error message format (`"Error: Failed to write .pyi stub …"`) and exit code; (b) that the already-written `.rs` is NOT cleaned up (the function comment claims "no partial artifacts" for generation errors but write errors are a different code path—`.rs` ends up on disk, `.pyi` does not, a partial state that no test documents or asserts); (c) that a raw Python `Exception` traceback is not leaked.
Fix: Add a test analogous to `test_cli_format_config_unreadable_exits_cleanly`. With `--protocol-module` provided, set `--pyi-output` to a path inside an existing directory (pass `str(tmp_path)` — a directory — as `--pyi-output`), invoke the CLI, and assert: `result.exit_code != 0`, `isinstance(result.exception, SystemExit)`, `"Error:" in result.output`, and (to document the partial-artifact behavior) `output_rs.exists()`.
