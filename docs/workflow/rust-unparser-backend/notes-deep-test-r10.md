Commit reviewed: fa22e182702d3ea1c1ec5e464345ab006941c9e9

test-1
File: tests/test_rust_unparser_parity_fixture.py:107 (_CORPUS)
What's wrong: `lval` and `rval` — the mutually indirect left-recursive rule pair — are absent from the parity corpus and from the native unparser tests. Both rules have generated unparser methods in both `unparser.rs` and `unparser_default.rs` (visible in the diff). The parser fixture's existing `native_tests.rs` already tests `lval`/`rval` parsing correctness, but the unparser side gets zero cross-backend or native coverage. Indirect mutual recursion produces a structurally distinct generated code shape from direct recursion (`expr`): the child enum dispatch for `lval` reaches `cst::LvalChild::Rval(shared)` calling `self.unparse_rval`, which then calls `self.unparse_lval`, two separate match arms in two separate methods rather than the single self-referencing `expr` path. A generator bug that scrambled the ordering of alternatives or the label check in mutual-recursion cases would not be caught.
Consequence: A regression in the generated code for mutual-recursion rules silently passes all parity tests and all native tests.
Fix: Add `("lval", "hello")` (base case), `("lval", "42!")` (indirect via rval), `("rval", "123")`, `("rval", "hello?")` to `_CORPUS`. Parseable texts: `lval` on `"42!"` gives inner:rval consuming `"42"` plus `"!"`, fully consuming; `rval` on `"hello?"` gives inner:lval consuming `"hello"` (as base:name) plus `"?"`.

test-2
File: tests/unparser_parity.py:85-93 (assert_unparse_parity)
What's wrong: When both backends return `None`, the first assertion `(py_str is None) == (rust_str is None)` evaluates to `True == True` and passes; the string equality check is never reached. There is no assertion that the Python backend (the reference implementation) succeeded. For all corpus entries the input is a complete valid parse (both `_py_cst` and `_rust_node` assert parse success), so the unparser should always return a non-None value. If a systematic generator fault caused both backends to emit code that returns `None` for some structurally valid CST — for example, a label enum variant mismatch that makes every child-extraction check fail — the test passes silently with neither string asserted.
Consequence: A mutual unparser failure on a valid parse input goes completely undetected; the test logs a passing result instead of surfacing the bug.
Fix: After calling `unparse_python`, add `assert py_str is not None, f"Python unparser returned None for valid parse rule={rule!r} text={text!r} w={max_width} i={indent_width}"`. The Python backend is the reference; if it fails on a successfully parsed input, that is a test infrastructure or generator bug that must surface loudly regardless of what the Rust backend does.

test-3
File: tests/rust_parser_fixture/src/native_tests.rs:1001 (use crate::unparser::Unparser)
What's wrong: All native GIL-free `#[test]` functions import `crate::unparser::Unparser` — the fltkfmt-configured variant. The `unparser_default.rs` module is declared and registered as a PyO3 submodule (`lib.rs` lines 9, 17 in the diff), but no `#[test]` exercises it without a Python runtime. The design §4 rationale for the native test is explicitly to "prove the fltk-unparser-core runtime and the generated pure-Rust Unparser link and run with no Python runtime." That proof is fulfilled only for the fltkfmt unparser; the default-config unparser's native linkage is unproven. A linker error or compile error in `unparser_default.rs` that is hidden by the `#[cfg(feature = "python")]` boundary would not surface until Python integration test time.
Consequence: A compile/link failure in `unparser_default.rs` in the `no python` feature configuration goes undetected by the native test suite.
Fix: Add at least one native `#[test]` that imports `crate::unparser_default::Unparser` and calls `render_native!` for one representative rule (e.g. `unparse_num` on `"123"`). This proves the module compiles and links with `fltk-unparser-core` in the GIL-free build.

test-4
File: tests/test_rust_unparser_parity_fixture.py:107 (_CORPUS)
What's wrong: The corpus contains `("opt_item", "1")` (optional item present) but not `("opt_item", "")` (optional item absent). The absent branch follows a different codegen path than the present branch and a different path than the `*` absent case already covered by `("zero_items", "")`. In `unparse_opt_item__alt0`, when the child is missing, `unparse_opt_item__alt0__item0` returns `None`, the `if let Some(r)` arm is skipped, and the method returns `Some(UnparseResult::new(initial_acc, 0))` — a non-None result with an empty doc. The `*` quantifier uses a loop whose body is never entered; the `?` quantifier uses an `if let` guard. These are distinct code paths in the generated Rust and in the Python unparser.
Consequence: A bug in the absent-`?` path (for example, returning `None` instead of `Some` when no child is present) would not be caught by any parity test.
Fix: Add `("opt_item", "")` to `_CORPUS`. Both backends should successfully unparse it to the empty string `""`.
