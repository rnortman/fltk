# User notes — requirements gate (chat directives, verbatim)

1. OK, we don't have to generate the .pyi unless the protocol is available. If it's being generated by the rust generator, then we automatically know it's there and we can generate the .pyi. Otherwise we have to be told what the import path is if we want a .pyi (and if neither then no .pyi).
