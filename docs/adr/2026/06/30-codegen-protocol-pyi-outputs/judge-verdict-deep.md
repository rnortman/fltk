# Judge verdict — deep review

Phase: deep. Base f0edfd75..HEAD 714fa9b5. Round 1.
Reviewers reviewed `19348b3a`; fixes committed on top (verified via `git diff 19348b3a..714fa9b5`). The diff contains exactly the claimed fixes — no scope creep.
Notes: 7 reviewer files; 9 findings across errhandling/quality/reuse/test (correctness, security, efficiency: no findings).

## Added TODOs walk

The only TODO added this phase comes from quality-4 (dispositioned "Fixed" by adding a TODO). Rubric applied independently.

### quality-4 — TODO(protocol-module-truthiness-gate) at gsm2tree.py:910 (gate) + TODO.md:101
Slug join verified: `TODO(protocol-module-truthiness-gate)` comment sits at the `if rule_name and self.py_module.import_path:` gate (gsm2tree.py:910), with a matching `## \`protocol-module-truthiness-gate\`` entry in TODO.md.
Q1 (worth doing): yes — `py_module` is dual-used as both the concrete-CST module path (annotation emission) and a bare truthiness sentinel for the protocol's `kind: Literal[NodeKind.*]` discriminant. A `Builtins`-backed generator silently emits the degraded `kind: object` form; the new Rust path works around it with a non-empty placeholder. The coupling is a real wart that each new caller would otherwise rediscover.
Q2 (design/owner input required): yes — this is the central, correctness-sensitive area of the whole change (design §1.2, cross-path byte-identity). Replacing the gate means deciding a new API shape (constructor flag vs `gen_protocol_module` param), choosing the default, and investigating whether the degraded `kind: object` form is still needed by any caller (it is reachable through `make_generator`/`pyreg.Builtins`) or can be deleted outright. Deleting/altering the degraded form is a behavior decision on a generated public-API surface (CLAUDE.md), warranting a deliberate design pass rather than an incidental in-PR refactor.
Not-silently-deferred check: the truthiness gate predates this iteration; this change added a design-anticipated, documented placeholder workaround (§1.2 called it out as an implementer hazard). The trap is surfaced for visibility at the gate location + TODO.md, satisfying the "fixed or escalated for visibility" requirement.
Assessment: passes both rubric questions, surfaced not buried → TODO acceptable. (Matches the quality reviewer's own recommendation, which asked for exactly this TODO.)

## Other findings walk

### errhandling-1 / quality-1 — Fixed (same change)
Claim: the `gen_rust_cst` write guard `if protocol_output is not None and protocol_text is not None:` is logically unreachable (any `generate_protocol()` error exits above; that call is the only assignment), so the double condition silently skips the write on a future control-flow defect instead of surfacing it — consequence is a silent partial output (Bazel reports success, `cst_protocol.py` absent, downstream `ModuleNotFoundError`, no log).
Diff at genparser.py:489-494: replaced with `if protocol_output is not None: assert protocol_text is not None; _write_output_file(...)`, mirroring the `init_pyi_text` writer below it, plus an explanatory comment.
Assessment: fix addresses the consequence at the named line — the invariant violation now raises `AssertionError` rather than eliding the write, and the silent-skip pattern is no longer present for future authors to copy. Both findings are the same issue; one change resolves both. Accept.

### reuse-1 / quality-3 — Fixed (same change)
Claim: the protocol-text formula `"# ruff: noqa: N802\n" + ast.unparse(...)` is duplicated verbatim across genparser.py (`generate --protocol`) and gsm2tree_rs.py (`generate_protocol`), with the noqa rationale (N802 kept; E501/F821 deliberately omitted) living only in the Python path — a latent drift hazard the byte-identity test only catches after the fact.
Diff: new `CstGenerator.gen_protocol_module_text()` (gsm2tree.py:800-816) carries the full noqa rationale in one docstring; `generate --protocol` (genparser.py:245) and `RustCstGenerator.generate_protocol` (gsm2tree_rs.py:450-453) both delegate to it; now-unused `import ast` removed from gsm2tree_rs.py.
Assessment: extraction collapses the two-file maintenance point to a single home with the rationale in one place — the finding's consequence is fully addressed. Responder placed the helper on `CstGenerator` rather than the design's suggested module-level `genparser.py` helper; the design (§2.2) explicitly left the factoring open with the byte-identity test as the guardrail, so this is an acceptable responder call. Byte-identity test (`test_gen_rust_cst_protocol_output_matches_python_protocol`) passes. Accept.

### quality-2 — Fixed
Claim: `render_stub_package_init` (gsm2lib_rs.py:56) hardcodes "Generated by `make gencode`" into the marker, but the Bazel `generate_rust_parser` rule drives the same CLI path, so every out-of-tree Bazel consumer receives a marker with false `make gencode` provenance.
Diff: gsm2lib_rs.py:56 changed to "Generated by gen-rust-* --init-pyi-output; do not edit by hand."; both committed in-tree markers (`fltk/_stubs/fegen_rust_cst/__init__.pyi`, `fltk/_stubs/rust_parser_fixture/__init__.pyi`) regenerated through the helper so the drift check stays clean.
Assessment: provenance is now accurate for all callers and the three sites (helper + two markers) are consistent, so `make gencode` drift stays green. Real consequence, addressed. Accept.

### test-1 — Fixed
Claim: the `_render_init_pyi` guard `if extension_name is None or submodules is None:` (genparser.py:559) was only exercised on the `submodules is None` side at the CLI level for both subcommands; the `extension_name is None` branch (submodules present, extension-name absent) was untested, so an argument-binding regression on the extension-name side would go undetected.
Diff: added `test_gen_rust_cst_init_pyi_requires_extension_name` + unparser variant — pass `--submodules` while omitting `--extension-name`, asserting non-zero exit, the exact message (`--init-pyi-output requires --extension-name and --submodules`, verified against genparser.py:560), and no files written.
Assessment: covers the previously-untested branch with a non-vacuous assertion (message string verified present). Both new tests pass. Accept.

### test-2 — Fixed
Claim: the malformed-identifier CLI tests only covered a bad `--submodules` entry; the parallel `_validate_rust_ident(extension_name, ...)` → `ValueError` → `typer.Exit(1)` wiring for a malformed `--extension-name` was untested at the CLI level (only the helper unit test existed), so swallowing that ValueError would go uncaught.
Diff: added `..._rejects_malformed_extension_name` (both subcommands) — pass `--extension-name bad-ext` (hyphen), asserting non-zero exit and nothing written.
Assessment: exercises the ValueError→Exit wiring for the extension-name path. Both tests pass. Accept.

### test-3 — Fixed
Claim: `..._requires_extension_name_and_submodules` test names claimed both options were tested while bodies only tested the missing-submodules case — misleading names concealing the test-1 gap.
Diff: renamed to `..._requires_submodules` (both subcommands) and the malformed tests to `..._rejects_malformed_submodule`, so each name matches the branch it exercises; the test-1/test-2 additions cover the newly-named cases.
Assessment: names now match behavior; the concealed gap is both renamed and filled. No production behavior was at issue (maintainer-misleading only). Accept.

## Approved

9 findings: 5 Fixed verified (errhandling-1, quality-1, quality-2, quality-3, reuse-1 — note quality-1/reuse-1 are duplicate issues of errhandling-1/quality-3, resolved by two underlying changes), 3 test fixes verified (test-1, test-2, test-3; 12 targeted tests pass), 1 TODO acceptable (quality-4 / protocol-module-truthiness-gate).

---

## Verdict: APPROVED

All dispositions acceptable. Fixes verified against the diff and the named lines; the added TODO passes both rubric questions and is surfaced (not silently deferred). Targeted test suite (12 tests, including the cross-path byte-identity guardrail) passes. HEAD 714fa9b5.
