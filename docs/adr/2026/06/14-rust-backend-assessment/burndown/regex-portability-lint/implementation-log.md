## Increment 1 — regex portability lint: grammar fix, portability checker, wiring, tests (commit TBD)

- `fltk/fegen/regex.fltkg`: Fixed F6 (&&-set-intersection over-admission from spike): excluded `&` from `class_char` terminal (`/[^\\\]\[\-\n&]/`). Updated stale `\z` comment (was "verified on both engines" — `\z` is Rust-only; comment now correctly documents the F3 finding and design rationale for admitting it). No `regex_subset.fltkg` created — the committed grammar IS the portable-subset grammar (naming deviation: design says `regex_subset.fltkg` but the source of truth is the already-committed `regex.fltkg`; design references are to the grammar by purpose, not name).
- `fltk/fegen/regex_parser.py`, `fltk/fegen/regex_trivia_parser.py`: Regenerated from updated `regex.fltkg` (F6 fix changes `class_char` terminal). The `regex_cst.py` was already up-to-date (CST shape unchanged; only the terminal regex value changed). Normalized by `make fix`.
- `fltk/fegen/regex_portability.py` (NEW): `RegexPortabilityIssue` dataclass + `check_regex_portable(pattern)` wrapper. Reports `error_tracker.longest_parse_len` as the offset (design §5.2 committed offset source). Empty pattern → portable (no construct). Hard fail and short parse both report via the same issue type.
- `fltk/fegen/gsm2parser_rs.py:1-42`: Rewrote module docstring to describe the two-engine semantic boundary (POSIX classes, `\p{}`, nested set ops, non-ASCII residual) and the new generation-time validator. Points at `fltk/fegen/regex.fltkg` and `regex_portability.py`.
- `fltk/fegen/gsm2parser_rs.py:50`: Added `from fltk.fegen.regex_portability import check_regex_portable`.
- `fltk/fegen/gsm2parser_rs.py:~772-783` (in `_gen_consume_term`, `gsm.Regex` branch): Added portability check before `_regex_idx`. Raises `ValueError` with pattern, offset, detail, and pointer to `regex.fltkg`. Check is at the user-term site only (not inside `_regex_idx`), keeping the domain to author-written patterns (design §5.3).
- `fltk/fegen/test_data/rust_parser_fixture.fltkg`: Added 9 new rules exercising portable-but-tricky regexes: `digit_seq` (`\d+`), `word_seq` (`\w+`), `ws_seq` (`\s+`), `three_to_five_digits` (`[0-9]{3,5}`), `exactly_two_digits` (`[0-9]{2}`), `escaped_metas` (`\.\*\+`), `latin_range` (`[À-Ö]+`), `nc_group_alt` (`(?:ab|cd)+`), `case_insensitive` (`(?i)[a-z]+`), `anchored_word` (`^[a-z]+$`).
- `tests/rust_parser_fixture/src/cst.rs`, `tests/rust_parser_fixture/src/parser.rs`: Regenerated from updated fixture grammar. All new regexes are portable (no rejection at generator time).
- `tests/test_regex_portability.py` (NEW): 116 tests. Portable patterns (41 parametrized cases covering the design §7 must-accept set). Non-portable patterns (22 parametrized cases covering design §7 must-reject set including F6-fixed `&&` cases). Offset-pinning tests for `[[:alpha:]]` and `(?=x)`. False-positive guards. Empty pattern. Generator integration (ValueError from Rust gen, no error from Python gen, portable-only grammar generates fine, motivating-bug regression). Whole-tree completeness (fegen.fltkg, rust_parser_fixture.fltkg, collision_fixture.fltkg — all patterns portable).
- `tests/test_rust_parser_parity_fixture.py`: Added 19 new parity corpus entries (digit_seq, word_seq, ws_seq, three_to_five_digits, exactly_two_digits, escaped_metas, latin_range, nc_group_alt, case_insensitive, anchored_word with SUCCESS/FAIL/PARTIAL cases).
- `tests/test_regex_grammar_adversarial.py`: Updated F6 cases from ACCEPT→REJECT (the F6 gap is now fixed); updated module docstring to document F6 as fixed.
- `tests/test_regex_grammar_corpus.py`: Updated expected terminal set for `class_char` (`[^\\\]\[\-\n&]`); updated `test_regex_fltkg_self_referential` to document the known non-portable class_char terminal and assert it intentionally; updated CLI smoke test to use fegen.fltkg (regex.fltkg now exits 1 due to its own class_char terminal); added `_REGEX_CORPUS_SKIP` to skip the known validator-internal non-portable terminal in the parametrized corpus sweep.
- `TODO.md`: Added `TODO(regex-unicode-class-divergence)` (non-ASCII `\d`/`\w`/`\s`/`\b`/`(?i)` residual ledger) and `TODO(regex-portability-target-list-drift)` (hand-copied target grammar list in test needs single-sourcing). Both have corresponding `TODO(slug)` comments at their locations.
- Makefile NOT edited: `regex.fltkg` and its generated artifacts were already in the Makefile's `gencode` recipe (added by the regex-grammar-spike). The design §5.1 add-to-gencode step was already done.
- Deviation: design says `regex_subset.fltkg` / `regex_subset_*.py` but the committed grammar uses `regex.*` names. This is documented here; no functional deviation — the grammar's purpose and content match the design exactly.
