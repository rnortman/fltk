# Judge verdict — deep review (burndown: cst-generated-header)

Phase: deep, skip-design fast-track. Base 205c36b..HEAD 9a6c963. Round 1.
Spec: `recommended-actions.md` §`cst-generated-header` (mirror `gsm2parser_rs.py:249-251`).
Notes: 7 reviewer files. Dispositions: 10 entries (correctness, security, test-1/2/3, reuse-1, quality-1/2, errhandling, efficiency).

## Added TODOs walk

All three duplication findings (reuse-1, quality-1, quality-2) map to a single
`TODO(cst-header-escape-dedup)`. Judged once; the slug is in sync — `TODO.md:41`
entry + comment at `gsm2tree_rs.py:153`.

### reuse-1 / quality-1 / quality-2 — TODO(cst-header-escape-dedup) at gsm2tree_rs.py:145-160
Q1 (worth doing): yes. Verified the divergence directly: `gsm2tree_rs._escape_source_name`
(`\`→`/`, `` ` ``→`'`) vs `gsm2parser_rs._gen_header` at `gsm2parser_rs.py:248` which
escapes the *same* `//!` backtick-span source name with `_rust_str_lit` (string-literal
encoder: over-escapes control chars/DEL, does not neutralise backticks). Two policies for
one context, plus structurally parallel `_gen_header` bodies differing only by tool name.
Real maintenance liability; ADR already flags emission-ir duplication.
Q2 (design/owner input required): yes. The prescribed fix extracts the escaper +
`_format_generated_header` to a shared module and routes *both* generators through it.
That forces resolving the policy mismatch (which encoder wins for the backtick context)
and editing `gsm2parser_rs._gen_header` — a separate generator whose `parser.rs` output
and fixtures sit outside this burndown item's scope. Not a mechanical single-site rename;
it is a cross-generator decision. Confirmed at base (205c36b) the parser `_gen_header` +
`_rust_str_lit` already pre-existed, so the duplication pattern is pre-existing, not a
regression this iteration introduced — this iteration's own `_escape_source_name` is
correct and well-formed for its context. The deferred concern is the cross-cutting
unification, which legitimately needs a design touch on a sibling generator.
Assessment: YES to both → TODO acceptable. Single TODO covering three findings is
correct; they describe one refactor. Disposition sound.

## Other findings walk

### test-1 — Fixed
Claim: with-source-name branch of `_gen_header` untested at unit level; `poc_source`
fixture passes no `source_name`, and the two `startswith`/substring assertions match both
branches identically. Consequence: a regression dropping the `from \`...\`` clause (or
writing the wrong name) escapes unit coverage.
Diff: `test_gsm2tree_rs.py` adds `test_header_with_source_name` (constructs
`RustCstGenerator(_make_minimal_grammar(), source_name="path/to/grammar.fltkg")`, asserts
exact full header line incl. `from \`...\``) and `test_header_without_source_name`
(asserts plain form + `"from \`" not in first line`). `_make_minimal_grammar` confirmed at
`test_gsm2tree_rs.py:105`. `test_preamble_at_start` also strengthened to exact
`startswith` of the full no-source-name header. Ran the suite: both new tests PASS.
Assessment: fix exercises the previously-uncovered branch and pins the exact line.
Accept.

### test-2 — Fixed
Claim: neither `_escape_source_name` branch (`\`→`/`, `` ` ``→`'`) is exercised; CI Linux
paths never carry these chars, so silent removal of either replace goes undetected.
Diff: new `TestEscapeSourceName` with four tests (plain unchanged, backslash, backtick,
combined). Import updated to bring in `_escape_source_name` (`test_gsm2tree_rs.py:20`).
Both escape branches now directly asserted. Ran: 4/4 PASS.
Assessment: directly covers both branches the finding named. Accept.

### test-3 — Fixed
Claim: `test_preamble_in_fegen_source` (multi-rule fegen path) checks many preamble
properties but never the header; inconsistent with the other preamble tests.
Diff: adds `assert fegen_source.startswith("//! Generated by fltk gen-rust-cst")` as first
assertion at the fegen test. Ran: PASS.
Assessment: closes the named consistency gap. Accept. (Reviewer rated this minor; the fix
matches the severity.)

### correctness-1 — Won't-Do ("No findings")
Reviewer file is literally "No findings." after tracing control/data flow, wiring, fixture
correctness, and a 245-test run. Verified independently: header is `parts[0]` before
`_preamble()`, joined with `\n`, string ends in `\n` → valid `//!` line + blank + first
item. Inner CST generator (`gsm2parser_rs.py`) constructs `RustCstGenerator(grammar)` with
no `source_name`, and never calls `.generate()` on it, so no stray/duplicate header leaks
into `parser.rs`. No consequence stated because none found.
Assessment: Won't-Do correct — no finding to address. Accept.

### security-1 — Won't-Do
Claim: `_escape_source_name` does not neutralise `\n`/`\r`; a source name with a newline
would terminate the `//!` comment early, emitting trailing text as Rust. Consequence:
malformed/compile-broken generated Rust.
Disposition rationale: reviewer explicitly classified this informational, not a finding.
`source_name` originates solely from `str(grammar_file)` (`genparser.py:336`), a developer
CLI argument in the same trust domain as the build; no trust boundary crossed; the parser
generator has the symmetric limitation. Verified the data flow: the only emission caller
threads the CLI path; no service/network/untrusted feed exists.
Assessment: per Severity calibration (Security), a finding is a blocker only when it shows
violation of a stated invariant on input crossing a trust boundary. Reviewer's own note
establishes input is developer-controlled, same trust domain — no boundary crossed. The
"defense-in-depth, optional" suggestion does not raise it to a TODO bar. Won't-Do sound.
Accept.

### errhandling — Won't-Do ("No findings")
Reviewer: "No findings." Diff is O(1) string formatting with no fallible paths beyond the
existing `ValueError` handling at the call site (unchanged). Nothing to address.
Assessment: Accept.

### efficiency — Won't-Do ("No findings")
Reviewer: "No findings." `_gen_header` runs once per `generate()` at codegen time, two
`str.replace` passes on a short path, outside any per-rule/per-node loop or hot path.
Assessment: Accept.

## Disputed items

None.

## Approved

10 dispositions: 3 Fixed verified (test-1/2/3, tests run and pass), 4 Won't-Do sound
(correctness, security-1, errhandling, efficiency), 3 findings folded into 1 acceptable
TODO (reuse-1, quality-1, quality-2 → cst-header-escape-dedup).

---

## Verdict: APPROVED

Every disposition holds. The three Fixed claims are real (new tests cover the previously
uncovered with-source-name and escape branches; verified passing). The four Won't-Do
calls each rest on either an explicit no-finding reviewer verdict or a same-trust-domain
input established by the security reviewer. The single TODO satisfies both rubric
questions — worth doing (genuinely divergent escape policies across two generators) and
requires cross-generator design work (touches the out-of-scope parser generator and forces
a policy-unification decision); the duplication is pre-existing, not introduced this
iteration. Spec satisfied: every committed `cst.rs` now carries the `//! Generated by fltk
gen-rust-cst ...` header mirroring the parser generator. Commit 9a6c963.
