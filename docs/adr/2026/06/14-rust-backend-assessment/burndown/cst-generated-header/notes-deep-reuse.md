reuse-1
File: fltk/fegen/gsm2tree_rs.py:145-151
What's duplicated: `_escape_source_name` performs backslash-to-forward-slash and backtick-to-apostrophe substitution to produce a source name safe for embedding in a Rust `//!` doc-comment backtick span.
Existing function: `_rust_str_lit` in fltk/fegen/gsm2parser_rs.py:31-44. That function escapes a string for safe embedding in a Rust *string literal* (double-quote context): it handles backslash (`\\`), double-quote (`\"`), control characters, and DEL. The two functions address different embedding contexts (backtick span vs. string literal), so neither is a strict superset of the other. However, the parser generator's `_gen_header` (gsm2parser_rs.py:245-264) also embeds `source_name` in a `//!` doc-comment backtick span and uses `_rust_str_lit` for that purpose (line 248) — so `gsm2parser_rs._gen_header` uses the *wrong* escaper for the backtick context (it over-escapes control chars and DEL, and does not neutralise backticks), while `gsm2tree_rs._gen_header` uses a purpose-written escaper that does the right thing for backticks. The two generators have parallel `_gen_header` methods doing the same logical job (emit a "Generated by ... Do not edit." Rust doc-comment with an optional source-name backtick span) but with divergent escape strategies.
Consequence: the escape logic for source names in backtick spans now lives in two places under different names and different contracts. If the set of characters that need neutralising in a backtick span expands (e.g. a decision to also neutralise `]` or newlines), both escapers must be updated independently. The mismatch is already semantically wrong in `gsm2parser_rs`: a source name containing a backtick would break the `//!` comment there but not in `gsm2tree_rs`. The shared "generate a do-not-edit header" pattern — identical in structure across both generators — is also duplicated: if the wording or format of the standard header comment changes, both `_gen_header` methods need updating. The correct fix is to extract `_escape_source_name` (or a renamed equivalent) to a shared module and have both generators use it, and to consider extracting a shared `_gen_do_not_edit_header(tool_name, source_name)` helper so the structural pattern has one home.
