# Dispositions — cst-generated-header deep review

Commit reviewed: 9372b2f. Fix commit: 9a6c963.

---

correctness-1 (implied "no findings"):
- Disposition: Won't-Do
- Action: no change
- Severity assessment: Reviewer confirmed no correctness issues; nothing to address.
- Rationale (Won't-Do): reviewer explicitly found no correctness findings after tracing control flow, data flow, wiring, fixture correctness, and test runs.

---

security-1:
- Disposition: Won't-Do
- Action: no change
- Severity assessment: `_escape_source_name` does not neutralize newlines in the `//!` comment, but the only production caller is `str(grammar_file)` from a developer-supplied CLI argument in the same trust domain as the build. A filesystem path with a literal newline is abnormal; the consequence is malformed generated Rust that the same developer compiles and inspects. No privilege boundary is crossed.
- Rationale (Won't-Do): Reviewer explicitly classified this as informational and not a finding. The input is developer-controlled, in the same trust domain as the build, and the practical path to a newline in a grammar file path is negligible. The parser generator has a symmetric limitation. Treating it as Won't-Do rather than a defensive TODO aligns with the reviewer's own framing and avoids noise for a non-issue.

---

test-1:
- Disposition: Fixed
- Action: `tests/test_gsm2tree_rs.py` — added `test_header_with_source_name` (constructs `RustCstGenerator(_make_minimal_grammar(), source_name="path/to/grammar.fltkg")`, asserts exact header line including `from` clause) and `test_header_without_source_name` (asserts plain form and absence of `from \`` on first line) inside `TestPreamble`. Both methods are at lines 230-250 after the existing `test_preamble_at_start`. Also strengthened `test_preamble_at_start` to use an exact `startswith` of the full no-source-name header line rather than two partial substring checks.
- Severity assessment: The `source_name` branch of `_gen_header` was covered only by the CLI end-to-end test; a regression dropping the `from \`...\`` clause from direct `RustCstGenerator` construction would not have been caught by the unit tests.

---

test-2:
- Disposition: Fixed
- Action: `tests/test_gsm2tree_rs.py` — added `TestEscapeSourceName` class (four tests: `test_plain_path_unchanged`, `test_backslash_replaced_with_forward_slash`, `test_backtick_replaced_with_apostrophe`, `test_backslash_and_backtick_combined`). Import updated to include `_escape_source_name` alongside `RustCstGenerator` at line 20.
- Severity assessment: Neither escape branch (backslash→forward-slash, backtick→apostrophe) was exercised by any test; Linux CI paths never contain these characters, so a silent removal of either replacement would go undetected.

---

test-3:
- Disposition: Fixed
- Action: `tests/test_gsm2tree_rs.py:665` (`test_preamble_in_fegen_source`) — added `assert fegen_source.startswith("//! Generated by fltk gen-rust-cst")` as the first assertion, consistent with the preamble invariant checked on `poc_source`.
- Severity assessment: Minor consistency gap; the header drop from multi-rule grammars was partially mitigated by `test_preamble_at_start`, but this test's own scope was internally inconsistent.

---

reuse-1:
- Disposition: TODO(cst-header-escape-dedup)
- Action: Added `TODO(cst-header-escape-dedup)` comment to `fltk/fegen/gsm2tree_rs.py` in the `_escape_source_name` docstring (lines 145-160). Added `cst-header-escape-dedup` entry to `TODO.md` describing the required shared-module refactor (extract `_escape_source_name` and `_format_generated_header(tool_name, source_name)` to a common module for both generators).
- Severity assessment: The two generators have divergent escape policies for the same doc-comment context and structurally identical `_gen_header` bodies. Future header wording changes or additional generators will propagate the duplication. Tracked as a deferred cleanup item rather than a respond-mode fix because it requires cross-file refactoring of both generators.

---

quality-1:
- Disposition: TODO(cst-header-escape-dedup)
- Action: Same TODO as reuse-1 — the TODO comment and TODO.md entry already capture both the escape-function and the `_gen_header` duplication concerns described in quality-1.
- Severity assessment: Duplicate escape policies increase maintenance surface; a third generator would propagate the pattern again. Deferred as technical debt, consistent with the ADR's existing `emission-ir-decision` note.

---

quality-2:
- Disposition: TODO(cst-header-escape-dedup)
- Action: Same TODO as reuse-1 and quality-1 — the existing entry covers the `_gen_header` structural duplication explicitly.
- Severity assessment: Header wording changes (tool rename, copyright, format) must be applied in two places with no mechanical enforcement. Tracked under the same slug as the escape-function concern since the fix is the same shared-module extraction.

---

errhandling (no findings):
- Disposition: Won't-Do
- Action: no change
- Severity assessment: Reviewer found no error-handling issues.
- Rationale (Won't-Do): explicit "No findings." verdict.

efficiency (no findings):
- Disposition: Won't-Do
- Action: no change
- Severity assessment: Reviewer found no efficiency issues.
- Rationale (Won't-Do): explicit "No findings." verdict.
