# User decisions on design.md §5 open questions (verbatim)

1. OQ-0 (conformance strategy): selected option (a) "Protocol-typed (Recommended)".
2. OQ-A (module-level Span gap): "Don't we have a common single Span defined somewhere? We should I think? that's how Python works -- the Span clas is not part of the generated CST module; it lives in a common lib."
3. OQ-1 (scope of Part 2): selected "Both now".
4. OQ-2 (Part 2 build strategy): selected "Prebuilt fegen_cst".
