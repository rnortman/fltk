reuse-1
File: fltk/fegen/gsm2tree.py:289–307 (`protocol_annotation_for_model_types`)
Duplicates: `py_annotation_for_model_types` at gsm2tree.py:85–93.
Both methods iterate `model_types`, build a sorted list of annotation strings, and return either the single element or `typing.Union[...]`. The only difference is the name-mapping step: the existing method resolves every type through `iir_type_for_model_type` → `pycompiler.iir_type_to_py_annotation`; the new method short-circuits `str` model types to `f'"{self.protocol_node_name(model_type)}"'` and calls `pycompiler.iir_type_to_py_annotation` only for non-string types. The structure is identical and diverges only in the rule-name → class-name transformation. A single parameterised method (e.g. `_annotation_for_model_types(model_types, rule_name_to_annotation)`) with a caller-supplied name mapper would unify both. As written, the Union-building logic and the sorting order are now maintained in two places; any future change (e.g. switching to `X | Y` syntax instead of `typing.Union`) must be applied twice.

reuse-2
File: fltk/fegen/gsm2tree.py:326–403 (`_protocol_class_for_model`)
Duplicates: `py_class_for_model` at gsm2tree.py:109–244.
Both methods receive `(class_name, model)` and emit the same public surface in the same order: optional nested Label class, `span`, `children`, `append`, `extend`, `child`, then per-label quintet (`append_<l>`, `extend_<l>`, `children_<l>`, `child_<l>`, `maybe_<l>`). The structural skeleton and ordering logic are identical; the delta is (a) the annotation resolver (`protocol_annotation_for_model_types` vs `py_annotation_for_model_types`), (b) the Label members (`ClassVar[object]` vs `enum.auto()`), (c) method bodies (`...` vs real statements), and (d) base class (`typing.Protocol` vs `@dataclass`). A shared skeleton with injected strategies for those four points would eliminate ~100 lines of parallel code. As written, adding a new per-label accessor (e.g. `count_<l>`) requires editing both generators; the Protocol generator will silently fall behind as concrete-class features evolve.

reuse-3
File: fltk/fegen/test_cst_protocol.py:1346–1368 (`run_pyright`)
Duplicates: no existing `run_pyright` helper exists in the repo today, so this is not a duplication finding. However, the helper is defined as a module-level function, not a pytest fixture or a shared utility, while the `pyright_available` fixture is session-scoped. If a second test module ever needs pyright harness logic, the function will be copy-pasted. This is a latent duplication risk, not a current violation; no finding issued.
