Concise. Precise. Complete. Unambiguous. No padding. Audience: smart LLM/human.

Commit reviewed: 0903a36

---

test-1
File: fltk/fegen/test_cst_protocol.py — T4 stand-in fixture (lines 312–339)
What: `_FakeGrammarNode` is assembled as a plain class but the test does NOT assert it structurally satisfies `GrammarNode` — it only `cast`s to it and then calls two methods. A `cast` never fails; the test can only pass for `any` fake class regardless of how many Protocol members are missing. This means the real B6 guarantee (Protocol imposes no dataclass-specific requirements) is not verified — a dataclass-specific member added to the Protocol (e.g., `__dataclass_fields__`) would not be caught. Consequence: T4's stated purpose ("Protocol is not dataclass-specific") is unverifiable by this test; a regression that added a dataclass-specific structural requirement would pass the test silently.
Fix: Replace the cast with a direct assignment `_node: cstp.GrammarNode = _FakeGrammarNode()` in the fixture and assert zero pyright errors — that forces a genuine structural match check. The cast should then move to a separate probe demonstrating what needs it (the Label nominal mismatch, i.e., the same pattern as T2b), not to the T4 positive case.

test-2
File: fltk/fegen/test_cst_protocol.py:364-384 — T5 (runtime import test)
What: The test skips if `fltk_cst_protocol` is already in `sys.modules` before the test runs. The test file itself imports `from fltk.fegen import gsm2tree` and `from fltk.fegen.genparser import _parse_grammar_raw, create_default_context`, and `fltk_cst_protocol` is imported at module scope by `test_plumbing.py` under `TYPE_CHECKING`, but in practice the T2a/T2b/T4 pyright fixture strings contain `from fltk.fegen import fltk_cst_protocol as cstp` — those fixtures run via subprocess pyright and do not import at runtime, so the protocol stays absent. However, within this *same process* the fixtures themselves call `cst_generator.gen_protocol_module()` and the module-level `PROTOCOL_MODULE = pathlib.Path(__file__).parent / "fltk_cst_protocol.py"` constant causes no import, so the skip guard would be hit only if another test in the session explicitly imports the module. The test does not use `importlib.reload` or a fresh subprocess. Consequence: If pytest collects test files in an order that causes another test to import `fltk_cst_protocol` (e.g., a future test), T5 would always skip and the invariant is never verified. The test's own comment acknowledges this; the fix is to run the import check in a subprocess or use `importlib` isolation. No regression now, but test is fragile to collection order.
Fix: Run `python -c "import fltk.fegen.fltk2gsm; import sys; assert 'fltk.fegen.fltk_cst_protocol' not in sys.modules"` as a subprocess call, eliminating the sys.modules state dependency.

test-3
File: fltk/fegen/test_cst_protocol.py — T1: test_cst_module_protocol_has_property_per_rule (lines 154-174)
What: The test asserts `CstModule` contains exactly the properties named `class_name_for_rule_node(r)` (i.e., `Grammar`, `Rule`, etc.). The design (section "New generated artifact") states `CstModule` must also expose any module-level names `Cst2Gsm` reads — specifically `Items`, `Item`, `Disposition`, `Quantifier` for label-comparison and isinstance dispatch (`self.cst.Items.Label.*`, `isinstance(item, self.cst.Item)`, etc.). The generated `CstModule` in `fltk_cst_protocol.py` does only expose these as `@property` (e.g. `Items`, `Item`, `Disposition`, `Quantifier`), which is correct since they coincide with the rule names. But the test asserts `expected_class_names == prop_names` where `expected_class_names` is derived purely from `rule_models` — this would fail to catch a future grammar renaming a rule away from the name `Cst2Gsm` accesses at runtime. Consequence: narrow gap — only matters if a future rule is renamed and `fltk2gsm.py` still references the old name. Not a current failure, but the test does not directly verify that the names `Cst2Gsm` specifically relies on (Items, Item, Disposition, Quantifier) are present.
Fix: Add four explicit assertions: `assert "Items" in prop_names`, `assert "Item" in prop_names`, `assert "Disposition" in prop_names`, `assert "Quantifier" in prop_names`. These double as regression guards if the grammar renames those rules.

test-4
File: fltk/fegen/test_cst_protocol.py — T2a _MEMBER_ACCESS_FIXTURE (lines 183-234)
What: The T2a member-access fixture only exercises three node types (`GrammarNode`, `ItemsNode`, `ItemNode`). The design (Test plan item 2a) requires "a fixture that... accesses node members through it (`m.Grammar`, `g.child_name()`, `m.Items.Label.NO_WS`, etc.)" and is intended to catch any genuinely-missing member the boundary cast would mask. The fixture omits `RuleNode` accessor calls (`child_name()`, `child_alternatives()`), `TermNode` accessors (`maybe_alternatives()`, `maybe_literal()`, etc.), `DispositionNode.child()`, `QuantifierNode.child()`, `LiteralNode.child_value()`, `RawStringNode.child_value()`, and `TriviaNode` members. The design explicitly calls these out as part of the B4 member-access guarantee. Consequence: a missing or misspelled member on any of the untested node types would not be caught by this test; the boundary cast for those nodes is justified only by the T2b probe (which checks assignability) rather than by direct member-resolution verification.
Fix: Add accessor calls for every node type present in `fltk_cst_protocol.py` to `_MEMBER_ACCESS_FIXTURE`: at minimum `RuleNode.child_name()`/`child_alternatives()`, `TermNode.maybe_literal()`/`maybe_identifier()`, `DispositionNode.child()`, `QuantifierNode.child()`, `LiteralNode.child_value()`, `RawStringNode.child_value()`.

test-5
File: fltk/test_plumbing.py:579 (cast added to `visit_grammar` call)
What: The test adds `cast("cstp.GrammarNode", result.result)` as the argument to `visit_grammar`. The comment says "nominal nested-Label mismatch; see _DEFAULT_CST in fltk2gsm.py" but this is the *test file's own call* in `test_plumbing.py`. The nested-Label mismatch applies at the `_DEFAULT_CST` binding in `fltk2gsm.py` (which is already guarded there), not at call sites that pass a concrete parse result. `result.result` is a `fltk_cst.Grammar` instance returned by the Python-backend parser — `fltk_cst.Grammar` is a concrete dataclass, and pyright should be able to resolve this as satisfying `cstp.GrammarNode` structurally (it has all required methods, and there's no nested-Label mismatch for the *instance* vs the *class*). Adding `cast` here hides any actual type mismatch on `result.result` (e.g. if the parser type annotation for `result.result` changed to `Any`). Consequence: if `result.result` degrades to `Any` in a future change, this cast masks it; also the justifying comment is misleading — the nested-Label mismatch is a module-level issue, not a per-node-instance issue.
Fix: Remove the cast and rely on pyright's structural matching for the concrete `fltk_cst.Grammar` instance vs `cstp.GrammarNode`. If pyright rejects the structural match, investigate and document why rather than silently casting. If the cast is truly needed, the comment must precisely state why `fltk_cst.Grammar` (an instance) does not structurally satisfy `GrammarNode` (a Protocol).

test-6
File: fltk/fegen/test_cst_protocol.py — design's Test plan item 2a explicitly required asserting wrong-but-existing label is *not* flagged
What: The design states: "Note explicitly: a wrong-but-existing label compared via `==` is *not* flagged (nominal-enum limitation) — assert this as a known boundary so the test does not falsely promise it." There is no such assertion or comment in `test_wrong_member_access_is_flagged` or `_MEMBER_ACCESS_FIXTURE`. The test only asserts the positive negative case (absent member is flagged) but does not document the known non-coverage of wrong-but-existing label comparisons. Consequence: future readers or callers may believe the Protocol provides full label-value safety when it does not; this gap could also silently regress if pyright someday does flag it (unexpected pass) or a refactor accidentally converts it to a missing-member case (unexpected fail).
Fix: Add a fixture string `_WRONG_LABEL_VALUE_FIXTURE` that calls `items.Label.ITEM == items.Label.NO_WS` (comparing valid-but-wrong labels) and asserts zero pyright errors — documenting the known non-coverage with a test-backed comment: "pyright does not flag valid-but-semantically-wrong label comparisons (nominal enum limitation; see design.md)."
