# Judge verdict — deep review (cst-python-feature-gate)

Phase: deep. Base e6a9117..HEAD 08dcf9f (fixes in 08dcf9f, reviewed commit 431ab53). Round 1.
Notes: 7 reviewer files (security: no findings); 16 findings; 13 Fixed, 1 Won't-Do, 2 TODO (one shared slug).

Style: concise, precise, no padding. Audience: smart LLM/human.

## Added TODOs walk

### reuse-2 + efficiency-2 — TODO(spike-gencode-dedup) at Makefile:132-133

Q1 (worth doing): yes — `make gencode` runs the generator twice on `poc_grammar.fltkg` producing byte-identical outputs (`src/cst_generated.rs`, `crates/fltk-cst-spike/src/cst.rs`); a `cp` removes one `uv run python` subprocess per regen and makes the identity invariant explicit instead of incidental.

Q2 (design cycle / owner input required): **no** — the TODO comment itself prescribes the exact fix ("Replace this separate gen step with a cp from src/cst_generated.rs"). The responder is not claiming the spike grammar may diverge (the efficiency reviewer's only keep-as-is condition); the existing Makefile comment already asserts "same grammar as cst_generated.rs". A two-line Makefile edit, no design question open, no owner input needed. Per rubric: clear NO to Q2 → do it now.

Aggravating, both per rubric and per repo convention:
- The redundant gencode step was **introduced in this iteration** (commit 5a53535 added it); a problem this iteration created cannot be deferred when it fails Q2.
- The slug is improperly registered: `TODO(spike-gencode-dedup)` exists only as the Makefile comment. `grep spike-gencode-dedup TODO.md` is empty — CLAUDE.md's TODO system requires both the `TODO.md` entry and the code comment.

Note: reuse-2's broader premise — that `crates/fltk-cst-spike/src/cst.rs` is "hand-authored, manually maintained" — is false, and the responder's rebuttal is correct: the file is generated by `make gencode` and the correctness reviewer independently verified it byte-matches fresh generator output. The legitimate residue of reuse-2 is exactly the gencode duplication (efficiency-2), judged above.

Assessment: disposition wrong. Do-now, not TODO.

## Other findings walk

### errhandling-1 — Fixed
Claim: `text_or_raise` `unwrap_or(src.len())` silently returns a too-long slice instead of raising on an impossible-but-unguarded boundary miss.
Evidence: span.rs:389-426 — `text_or_raise` now validates pre-conditions (no source / negative / inverted / end > char_count, each with its original message) then delegates to native `text()` with `ok_or_else` producing the start-OOB ValueError. The `unwrap_or` and the duplicate `char_indices` walk are gone. Traced the residual delegation: with all guards passed, `text()` returns `None` only for `start == end == char_count` on a non-empty source — which old code also rejected with the same "start index out of bounds" message. One Python-visible edge-case change: span(0,0) on an empty source now returns `""` instead of raising — this makes `text_or_raise` consistent with `text()` (which already returned `Some("")`) and with the documented contract "same conditions as text()"; no test pinned the old inconsistency.
Assessment: fix addresses the finding and removes the duplication. Accept.

### errhandling-2 — Fixed
Claim: `py_merge`/`py_intersect` discard the `SpanError` discriminant with `|_|`; future variants of the `#[non_exhaustive]` enum reach Python with a misleading message.
Evidence: span.rs:454, 464 — both now `map_err(|e| PyValueError::new_err(e.to_string()))`. `SpanError::Display` (span.rs:23-29) emits exactly "cannot merge spans from different sources" for `SourceMismatch`, so the Python-pinned message is byte-identical.
Assessment: matches the reviewer's suggested fix verbatim. Accept.

### errhandling-3 — Won't-Do
Claim: latent asymmetric match structure in `text()` (`(None, Some(0)) if start == 0` arm); reviewer explicitly stated "No separate action required unless `text()` is changed."
Rationale: `text()` was rewritten this round (efficiency-1); the asymmetric arm no longer exists.
Inspection: span.rs:248-277 — single-pass implementation; the empty-source case is now an explicit, commented early return (line 270-273); the flagged match arm is gone.
Assessment: rationale is true; the concern is structurally resolved, and the reviewer pre-conditioned action on `text()` changing — which happened, with the fix. Accept.

### correctness-1 — Fixed
Claim: guide's Cargo.toml template uses a crates.io version dep for an unpublished crate; consumer copying it gets resolution failure. Adjacent nit: lint named `unexpected_cfg`.
Evidence: rust-cst-extension-guide.md:58-62 — commented `path` and `git` dependency forms added alongside the version dep with "no crates.io release yet" note; line 145 lint name corrected to `unexpected_cfgs`.
Assessment: both points addressed. Accept.

### test-1 — Fixed
Claim: python-off duplicate block asserted only for `NodeKind` and `Identifier_Label`; `Items_Label`/`Trivia_Label` uncovered.
Evidence: tests/test_gsm2tree_rs.py:468-475 — both exact-string assertions added, matching the reviewer's requested form. Suite: 120 passed.
Assessment: accept.

### test-2 — Fixed
Claim: `TriviaChild` (span-only child enum, distinct `_child_enum_block` path) missing from gated-impl and PartialEq tests.
Evidence: `'#[cfg(feature = "python")]\nimpl TriviaChild {'` assertion added (line 437); PartialEq check now loops over `IdentifierChild` and `TriviaChild` (lines 442-452). The loop shares the pre-existing vacuous-pass shape (no assertion that the impl exists at all), but that matches the reviewer's requested "parallel to the existing IdentifierChild check" and the gated-impl test independently asserts the Trivia impls exist.
Assessment: accept.

### test-3 — Fixed
Claim: duplicate assertion in `TestRegisterClasses.test_fegen_register_classes_gated` and `TestFegenGrammar.test_register_classes_present`.
Evidence: the `TestRegisterClasses` copy removed (diff at tests/test_gsm2tree_rs.py:394-399); assertion retained in `TestFegenGrammar` per the reviewer's placement.
Assessment: accept.

### test-4 — Fixed
Claim: merge/intersect unit tests assert coordinates only, never `text()`; silent source-arc drop undetectable at unit level.
Evidence: lib.rs:154 `assert_eq!(merged.text(), Some("hello world".to_string()))`; lib.rs:185 `assert_eq!(inter.text(), Some("lo w".to_string()))` — `"hello world"[3:7]` is `"lo w"`, correct. `cargo test -p fltk-cst-core --no-default-features`: 24 passed.
Assessment: accept.

### test-5 — Fixed
Claim: `end == 0` fast path in `text()` untested.
Evidence: lib.rs:138-144 `span_text_zero_to_zero` — `Span::new_with_source(0, 0, &src)` on `"hello"`, asserts `Some("")`. Pins the behavior across the efficiency-1 rewrite (new path: first loop iteration sets both offsets to 0).
Assessment: accept.

### reuse-1 — Fixed
Claim: merge-message string hand-spelled in three places; `SpanError::Display` introduced as source of truth then unused.
Evidence: same fix as errhandling-2 — `e.to_string()` at span.rs:454, 464; `Display` is now the single encoding of the message.
Assessment: accept.

### quality-1 — Fixed
Claim: `text_or_raise` duplicates the codepoint→byte translation independently from `text()`, with semantic-drift risk.
Evidence: same fix as errhandling-1 — `text_or_raise` is now a validation layer delegating to `text()` for the slice (span.rs:419-425); the second `char_indices` walk is gone.
Assessment: accept.

### quality-2 — Fixed
Claim: dual-cfg enum blocks copy variant lists via two independent loops; single-loop edits produce mode-mismatched enums.
Evidence: gsm2tree_rs.py — `variant_names` extracted once before the NodeKind pair (lines 326-328), `label_variants` before the label-enum pair (line 393); both python-on and python-off loops iterate the shared list. Generator-output tests (120) pass; output unchanged (pure refactor of emission).
Assessment: accept.

### quality-3 — Fixed
Claim: `_label_from_pyobject_match` default `method_name="append"` hides caller coupling.
Evidence: gsm2tree_rs.py:717 — default removed from signature; the `append` call site now passes `method_name="append"` explicitly (line 709); the `extend` site already passed it.
Assessment: accept.

### efficiency-1 — Fixed
Claim: `text()` does up to three full linear scans (two `nth()` restarts + `chars().count()` fallback firing for every end-of-source span); O(source_len) per span read.
Evidence: span.rs:248-277 — single forward `char_indices` pass collecting both byte offsets, early `break` at `end`, exhaustion-with-`char_count == end` handling the end-of-source case without a separate scan. Worst case O(end). Behavior preservation traced across all edge cases (empty source, start==end mid-string, start==end==char_count, inverted/OOB → None unchanged); 24 core unit tests + 19 spike tests + 120 generator tests green.
Assessment: matches the reviewer's prescribed single-pass fix. Accept.

## Disputed items

- **reuse-2 / efficiency-2 — TODO(spike-gencode-dedup)**: fails rubric Q2 (the TODO prescribes its own two-line fix; no design or owner input needed) and the duplication was created this iteration. Need: apply the `cp` in the `gencode` target and delete the TODO comment. If the responder instead asserts the spike grammar is expected to diverge (the reviewer's keep-as-is condition), say so explicitly and delete the TODO as moot — either way the TODO goes. If any TODO survives, it must also gain its missing `TODO.md` entry (repo TODO-system requirement).

## Approved

14 of 16 findings: 13 Fixed verified, 1 Won't-Do sound.

---

## Round 1 verdict: REWORK

One disposition wrong (reuse-2/efficiency-2 TODO). Round 1.

---

# Round 2

Phase: deep. Base e6a9117..HEAD 07b4f2c (round-2 fix in 07b4f2c). Round 2 — APPROVED or ESCALATE only.
Scope: only the round-1 disputed item; the other 14 findings were approved in round 1 and 07b4f2c touches only `Makefile` + the dispositions doc, so nothing approved is disturbed.

## Disputed item re-walk

### reuse-2 + efficiency-2 — was TODO(spike-gencode-dedup), now Fixed
Required: apply the `cp` in the `gencode` target and delete the TODO comment.
Evidence (commit 07b4f2c, Makefile):
- The second `$(MAKE) gen-rust-cst GRAMMAR=...poc_grammar.fltkg RS_OUT=crates/fltk-cst-spike/src/cst.rs` invocation is replaced by `cp src/cst_generated.rs crates/fltk-cst-spike/src/cst.rs`, exactly the fix the TODO prescribed and the round-1 verdict demanded.
- Ordering verified: `src/cst_generated.rs` is generated earlier in the same `gencode` target, so the `cp` copies fresh output.
- The TODO comment is gone and `git grep spike-gencode-dedup 07b4f2c` is empty — no orphan slug, so the missing-`TODO.md`-entry defect is moot.
- `diff <(git show 07b4f2c:src/cst_generated.rs) <(git show 07b4f2c:crates/fltk-cst-spike/src/cst.rs)` — byte-identical at HEAD; the identity invariant the `cp` encodes holds.
Assessment: disposition now correct. Accept.

## Approved

16 of 16 findings: 15 Fixed verified (incl. reuse-2/efficiency-2 promoted from TODO), 1 Won't-Do sound. No TODOs remain.

---

## Verdict: APPROVED
