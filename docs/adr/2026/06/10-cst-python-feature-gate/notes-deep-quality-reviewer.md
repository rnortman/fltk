quality-1
File: crates/fltk-cst-core/src/span.rs:378-419
`text_or_raise` duplicates the codepoint-to-byte-offset translation logic independently from `text()` (lines 237-267). Both methods walk `char_indices` to translate codepoint indices; `text_or_raise` does not delegate to `text()`. The logic is similar but not identical — the byte-offset computation uses a different special-case structure — creating a semantic drift risk.
Consequence: Any future change to codepoint handling in `text()` must be replicated in `text_or_raise`; no compiler or test will enforce this. The two methods are already subtly asymmetric (e.g. the `(None, Some(0)) if start == 0` special case in `text()` is absent in `text_or_raise`, which instead does a bounds-first check). Over time, the error messages in `text_or_raise` describe conditions while the underlying byte-offset logic drifts, making bug reports harder to diagnose.
Fix: Rewrite `text_or_raise` to delegate to `text()` for the actual slice: validate the pre-conditions explicitly (returning the structured `PyValueError` messages as now), then call `self.text().unwrap()` once the pre-conditions are confirmed valid. This eliminates the duplicated `char_indices` walk and makes `text_or_raise` a thin error-message layer over `text()`.

quality-2
File: fltk/fegen/gsm2tree_rs.py:313-342 (NodeKind block), 384-415 (label enum block); generated output in crates/fltk-cst-spike/src/cst.rs, src/cst_fegen.rs, src/cst_generated.rs, tests/rust_cst_fixture/src/cst.rs
Each enum is emitted twice — a python-on block and a python-off block — with identical variant lists copied inline by two separate `for` loops. The comment at line 316 correctly explains why `cfg_attr` on variant helper attributes fails with pyo3 0.23, but does not eliminate the copy-paste maintenance burden.
Consequence: Adding, renaming, or reordering a label variant requires the generator to emit both copies. A future generator change that updates only one loop produces generated code that compiles in one mode but mismatches the other. The two loops are not structurally linked; they are textually adjacent, and the separation will grow as the enum blocks gain more attributes or comments. Grammars with large label sets (e.g., fegen's 14-class grammar) already produce 14 pairs.
Fix: Extract the variant list into a local variable before the two `lines.append` loops, and loop over the shared variable in both the python-on and python-off blocks. The comment explaining the pyo3 constraint stays — only the duplication of the data (variant names) is eliminated.

quality-3
File: fltk/fegen/gsm2tree_rs.py:712 (`_label_from_pyobject_match` signature)
The `method_name` parameter added to `_label_from_pyobject_match` defaults to `"append"`, but the callers at lines 751 and 752 pass it only at the second call site (`method_name="extend"`); the first call site at line 744 silently relies on the default `"append"`. The error message produced for `append` is correct today, but the coupling between the default and the caller is invisible.
Consequence: If a future caller passes neither value, the error message silently uses `"append"` regardless of the actual method name, which misleads downstream consumers debugging type errors. The default preserves the old behavior but as an implicit contract rather than an explicit one.
Fix: Remove the default value; require all callers to pass the `method_name` argument explicitly. There are exactly two call sites, so the change is mechanical. This makes the coupling visible at both call sites.
