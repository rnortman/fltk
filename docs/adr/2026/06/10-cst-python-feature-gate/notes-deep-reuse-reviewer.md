Style: concise, precise, no padding. Audience: smart LLM/human.

Commit reviewed: 431ab53

---

## reuse-1

**File:line:** `crates/fltk-cst-core/src/span.rs:447` and `:457`

**What's duplicated:** The string literal `"cannot merge spans from different sources"` is hardcoded twice in the Python wrapper methods `py_merge` and `py_intersect` (the `map_err(|_| PyValueError::new_err(...))` calls).

**Existing function/utility:** `SpanError::Display` at `crates/fltk-cst-core/src/span.rs:23-28` already encodes this exact string. The closures discard the `SpanError` value (`|_|`) and re-spell the message by hand instead of using `e.to_string()` or `format!("{e}")`.

**Consequence:** The three locations (`SpanError::fmt`, `py_merge`, `py_intersect`) that all encode the same user-visible message will diverge independently as the error taxonomy grows (the type is `#[non_exhaustive]`). A future `SpanError` variant added to `coerce_source` will silently reach Python with the wrong "cannot merge spans" text. The `SpanError::Display` impl was introduced in this same diff specifically to be the single source of truth for this message, then not used where it was meant to apply.

---

## reuse-2

**File:line:** `crates/fltk-cst-spike/src/cst.rs` (entire file, 1391 lines)

**What's duplicated:** `fltk-cst-spike/src/cst.rs` is a hand-authored, grammar-specific file that mirrors the structure the generator (`fltk/fegen/gsm2tree_rs.py`) emits for any grammar. It contains three complete node types (`Identifier`, `Items`, `Trivia`) with their label enums, child enums, native impls, and Python-gated `#[pymethods]` blocks, all following the exact same pattern — including the `extend`, `extend_children`, per-label accessors, `__eq__`, `__hash__`, `__repr__` — that `gsm2tree_rs.py` emits via `_generic_extend`, `_generic_extend_children`, `_per_label_methods`, etc.

**Existing function/utility:** `fltk/fegen/gsm2tree_rs.py:_generic_extend` (line 744), `_generic_extend_children` (line 768), `_per_label_methods` (line 800+), `_emit_node_class` (line 580+). The test fixture `tests/rust_cst_fixture/src/cst.rs` (same commit) serves the same structural role and is already generated by running the generator against a fixture grammar.

**Consequence:** The spike's `cst.rs` is a separate, manually maintained copy of a code pattern that will evolve with the generator. Changes to the generated structure (new native methods, changed error messages, new cfg gates) require parallel edits to both `cst.rs` files (spike + fixture) and the generator. The spike was introduced as a "pure-Rust proof-of-concept" and its `spike_tests.rs` explicitly describes it as exercising what the generator would emit — the fixture path (`tests/rust_cst_fixture`) already covers that role for the generator; the spike crate duplicates it at the structural level.
