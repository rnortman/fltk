Style: concise, precise, complete, unambiguous. No padding.

Commit reviewed: 912b285

---

test-1
File: tests/test_rust_span.py:328–335 (`test_with_source_unchecked_non_str_marker_raises_type_error`)
What: Test asserts only `match="_fltk_cst_core_abi"` — the attribute name — which was already present in the old message. It does not pin the new unified template. The design says template 2 now reads `"{label} ABI mismatch: {subject}._fltk_cst_core_abi is {type}, not str"`, and every other updated test in this diff adds assertions for `"SourceText ABI mismatch"` and the new template's distinguishing substrings. This one was not updated.
Consequence: The non-str-marker error path on the SourceText branch could silently regress to any message containing `"_fltk_cst_core_abi"` (including the old pre-refactor wording) and the test would still pass. Parallel to the deliberate-change tests for template 1, 4, 5, which all received strengthened assertions in this diff.
Fix: Add `with pytest.raises(TypeError, match="SourceText ABI mismatch") as exc_info:`, then assert `"not str" in msg` (and optionally `"_fltk_cst_core_abi is" in msg`) on `str(exc_info.value)`, matching the style of `test_source_text_abi_layout_non_int_raises`.

---

test-2
File: tests/test_rust_span.py — `TestSpanPathAbiGate` subprocess suite
What: Five subprocess scenarios exist for the Span path: control/pass, string-mismatch, layout-mismatch, missing-marker (template 1), and missing-layout-attr (template 4). Templates 2 (non-str marker) and 5 (non-int layout attr) on the Span path have no subprocess test. The design notes that templates 2 and 5 now carry the added `"Span ABI mismatch: "` prefix (previously those two Span-path messages had no prefix). After refactoring, the Span path's templates 2 and 5 are generated by `check_abi_pair` and are structurally different from the old code.
Consequence: A regression in the `check_abi_pair` step-2 or step-6 branches (e.g. wrong prefix, missing label, wrong attribute-type interpolation) on the Span path would not be caught. These are lower-probability scenarios but the helper now owns the message for all six templates on both paths; only four of six are exercised via subprocess.
Fix: Add two subprocess tests to `TestSpanPathAbiGate` — one patching `fltk._native.Span._fltk_cst_core_abi` to a non-str (e.g. `42`), asserting `"Span ABI mismatch"` and `"not str"` in the message; one patching `_fltk_cst_core_abi_layout` to a non-int (e.g. `"oops"`), asserting `"Span ABI mismatch"` and `"not int"`. These scenarios are straightforward to craft following the existing subprocess test pattern.

---

test-3
File: tests/test_rust_span.py — `TestAbiMarkerClassattr` / `test_with_source_unchecked_bogus_abi_marker_raises_type_error` (line 337)
What: Test pins `"FakeSource"` in the message, validating `py_type_obj_name()` derivation. The class is defined as a bare `class FakeSource` inside a test method. The design notes (§2) that the stripping rule applies to `"builtins"` and `"__main__"` but not to test modules — so `FakeSource` defined inside a test function in `test_rust_span.py` will render as something like `"test_rust_span.TestAbiMarkerClassattr.test_with_source_unchecked_bogus_abi_marker_raises_type_error.<locals>.FakeSource"`. The assertion `"FakeSource" in msg` is a substring check, so it passes whether the full path or bare name appears. This is fine as written — the design explicitly says the assertion pins the name, not the full path. No gap here; noted for clarity that the assertion is intentionally loose.
Consequence: None — substring check is sufficient to pin the intent.
Fix: No fix needed.

---

No findings. (test-3 is a non-finding clarification; substantive gaps are test-1 and test-2.)
