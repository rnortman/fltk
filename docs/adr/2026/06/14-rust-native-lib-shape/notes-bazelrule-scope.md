scope-1: BUILD.bazel:118 — stale comment says "gen-rust-lib genrule" after the genrule was replaced by `generate_rust_lib` proper rule. Expected: comment names the Starlark rule, not a genrule. Consequence: comment misleads future readers about the mechanism; no functional impact. Fix: change "gen-rust-lib genrule" to "generate_rust_lib rule" (or similar).

All other spec items confirmed present and complete: `generate_rust_lib` rule with `ctx.actions.run` (rust.bzl:23-82); `_gen_tool` Label-defaulted executable attr (rust.bzl:68-74); typed `module_name` + bool flag attrs with no shell interpolation; old `native.genrule` block fully replaced with no dead remnants; `TODO(bazel-lib-rs-location)` entry removed from TODO.md; inline TODO comment removed with the genrule block; clockwork + bootstrap_native confirmed building per implementation log.
