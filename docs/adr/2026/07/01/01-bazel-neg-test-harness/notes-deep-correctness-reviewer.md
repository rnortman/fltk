No findings. (Verified at 9afab45: guard refactor is behavior-preserving — identical conditions/ordering/messages/firing phases, both `_require_protocol_module` call sites intact; ran `bazel test //tests/bazel_rules:rust_bzl_tests` (11/11 pass), `bazel build //tests/bazel_rules/...` (manual tag skips the negative target), and a guard-disable mutation of the `deps` knob tuple, which failed exactly the corresponding unit test.)
