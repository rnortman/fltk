Concise. Precise. Complete. Audience: smart LLM/human. No padding.

Commit reviewed: cdffac4

---

test-1: `fltk/test_plumbing.py:467` and `fltk/test_plumbing.py:496` — `test_rust_backend_missing_module_hard_errors` and `test_rust_backend_no_python_exec_fallback` are byte-for-byte identical in body (both call `generate_parser` with a nonexistent module name, both assert `module_name not in sys.modules`). The second test adds no distinct behavior or assertion. Consequence: a regression (e.g., sys.modules IS polluted in one failure mode) would be caught equally by either test; neither distinguishes the two failure modes the names imply. Fix: delete `test_rust_backend_no_python_exec_fallback`; its coverage is wholly subsumed. If the "no Python-exec fallback" invariant is separately desired, assert it as `pr.cst_module_name not in sys.modules` AND confirm no parser was exec'd (e.g., mock `exec` and assert it was not called).

test-2: `fltk/test_plumbing.py:545` — `test_parse_grammar_rust_missing_module_no_fallback` wraps `pytest.raises` logic in a manual try/except with a sentinel variable. If the code under test raises a *different* exception (not `RustBackendUnavailableError`), the test silently passes with `result is None`. Consequence: a regression where the wrong exception type is raised goes undetected. Fix: `with pytest.raises(RustBackendUnavailableError): parse_grammar(...)` — let pytest do the assertion.

test-3: `tests/test_phase4_fegen_rust_backend.py:78` and `tests/test_phase4_fegen_rust_backend.py:88` — `test_fegen_grammar_itself_rust_equals_python` and `test_parse_grammar_file_rust_backend` have identical bodies (both call `parse_grammar_file(_FEGEN_FLTKG_PATH, ...)` and assert equality). The second test's docstring claims to test "threads rust_fegen_cst_module through correctly" but is observationally identical to the first. Consequence: zero additional coverage; one of them can hide a name-collision failure silently. Fix: delete `test_parse_grammar_file_rust_backend`. If threading is a distinct concern, assert it at the `parse_grammar` level (which `parse_grammar_file` calls) rather than duplicating at the `parse_grammar_file` level.

test-4: `fltk/test_plumbing.py:508` — `test_rust_backend_uses_provided_classes` mocks `importlib.import_module` to return a module whose only class is `FakeNode` but then calls `generate_parser(grammar, ...)` against a grammar rule `fake_node`. The generated parser exec will look up `FakeNode` in `parser_globals`; since `FakeNode` has no attributes matching a CST node API, the exec itself may succeed but actual parsing with the returned `parser_class` would fail. The test asserts `pr.parser_class is not None` only implicitly (via `pr.cst_module_name in sys.modules`). Consequence: `pr.parser_class` is not asserted to be non-None; if `generate_parser` returned a `ParserResult` with a `None` parser class, the test would still pass (the `in sys.modules` assertion would still hold). Fix: add `assert pr.parser_class is not None`.

test-5: `tests/test_phase4_rust_fixture.py:175` — `test_ac3_span_read`'s comment states "The Rust fltk._native.Span intentionally does NOT expose .start/.end as Python attributes". However, `fltk2gsm.Cst2Gsm` calls `span.start` and `span.end` directly at lines 26, 136, and 140 of `fltk2gsm.py` on the result of `identifier.child_name()`, `literal.child_value()`, and `regex.child_value()`. These return `Span`-like sub-nodes (fegen CST nodes, not `fltk._native.Span`), so the comment is not wrong — but the test's claim that it "verifies what the real consumer contract requires" is incomplete: it does not test that child_name(), child_value() return objects with `.start`/`.end`, which are the actual spans consumed by `Cst2Gsm`. The AC8 tests exercise this indirectly through `parse_grammar` equality, but there is no focused test that a Rust-backed `Identifier.child_name()` or `Literal.child_value()` returns a span-like with `.start` and `.end`. Consequence: a regression where Rust `child_name()`/`child_value()` returns an object without `.start`/`.end` would only be caught in the full AC8 parse path, making root-cause diagnosis harder. Fix: add a direct test that calls `node.child_name()` (or `child_value()`) on a Rust-backed node and asserts `.start` and `.end` attributes are accessible and correct.

test-6: `tests/test_gsm2tree_rs.py:207` — `assert "UNKNOWN_SPAN\n" not in poc_source` uses a literal newline in the test string. This correctly distinguishes `UNKNOWN_SPAN\n` (old pattern split across lines) from `UNKNOWN_SPAN_CACHE\n` (new pattern, where the suffix `_CACHE` prevents the match). However this is a fragile string-pattern negative assertion: it passes vacuously if the old code ever reformatted the Rust emission without a newline after `UNKNOWN_SPAN` (e.g., if it were `UNKNOWN_SPAN.get`). A stronger assertion is already present at line 208 (`assert "UNKNOWN_SPAN.get(py)" not in poc_source`), which is the true behavioral invariant. Consequence: line 207's assertion is nearly redundant with the `.get(py)` check and could pass even if the old coupling re-appeared in a different form. Fix: replace `assert "UNKNOWN_SPAN\n" not in poc_source` with `assert "\nuse crate::UNKNOWN_SPAN" not in poc_source` (the actual import form that would re-introduce the linkage), or drop line 207 as fully covered by line 208 and the `use crate::UNKNOWN_SPAN;` check at line 196.

test-7: `fltk/test_plumbing.py:223`-area — `generate_parser` registers `sys.modules[module_name] = cst_module` at line 223, which is BEFORE the `exec(compile(parser_module, ...))` at line 240. If the parser exec raises (e.g., due to a malformed generated parser AST), `sys.modules` is left polluted with the per-call module even though no usable parser was returned. No test covers this. The design doc's AC4 argument ("The raise happens before `sys.modules[module_name]` is set") is only true for the `_load_rust_cst_classes` failure path, not for a parser-codegen failure. Consequence: a bug in parser codegen leaves `sys.modules` dirty and breaks subsequent calls that reuse the same `id(grammar)`. Fix: add a test that forces parser exec to fail (e.g., monkeypatch `exec` to raise) and asserts `module_name not in sys.modules`; if it fails, move `sys.modules[module_name] = cst_module` to after the exec or wrap both in a try/finally.
